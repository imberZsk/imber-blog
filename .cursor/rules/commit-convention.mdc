---
alwaysApply: true
---

# 约定式提交 1.0.0

## 重要提示

**当你需要生成 Git 提交信息时，必须严格遵循以下约定式提交规范。**

⚠️ **关键要求：提交信息的描述部分（冒号后面的内容）必须使用中文，禁止使用英文！**

### 提交信息格式要求

所有提交信息必须使用以下格式：

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

**注意：`<description>` 和 `<body>` 部分必须使用中文！**

### 正确示例

✅ **正确**：

- `feat: 添加用户登录功能`
- `fix: 修复登录页面验证错误`
- `refactor: 重构用户认证模块`
- `docs: 更新 README 文档`

❌ **错误**：

- `feat: add user login feature` （使用了英文）
- `fix: fix login validation error` （使用了英文）

### 提交类型选择指南

在生成提交信息时，根据代码变更内容选择合适的类型：

- **feat**: 新增功能（新功能、新组件、新特性）
- **fix**: 修复 bug（修复错误、修复问题）
- **docs**: 文档变更（README、注释、文档更新）
- **style**: 代码格式调整（缩进、空格、格式化，不影响代码逻辑）
- **refactor**: 代码重构（重构代码结构，不改变功能）
- **perf**: 性能优化（提升性能、减少内存占用）
- **test**: 测试相关（添加、修改测试用例）
- **build**: 构建系统变更（依赖、构建工具、配置）
- **ci**: CI/CD 变更（持续集成配置）
- **chore**: 其他变更（工具配置、非业务代码）

### 提交信息生成规则

1. **必须**提交信息中，类型前缀（如 `feat:`、`fix:`）后面的描述部分必须使用中文，禁止使用英文
2. **必须**以类型开头，后跟冒号和空格
3. **必须**使用小写类型（如 `feat`, `fix`）
4. **可以**添加范围（如 `feat(parser):`），但范围后的描述仍必须使用中文
5. 描述应该简洁明了，不超过 50 个字符（标题行），且必须使用中文
6. 如果需要详细说明，在空行后添加正文（正文也必须使用中文）

### 语言要求重申

- ✅ **允许**：类型前缀使用英文（`feat:`, `fix:`, `refactor:` 等）
- ✅ **必须**：描述部分使用中文
- ❌ **禁止**：描述部分使用英文
