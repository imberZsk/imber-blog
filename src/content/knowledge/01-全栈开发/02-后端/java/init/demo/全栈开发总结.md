## 环境

### jdk 版本

看自己电脑

`java -version：openjdk version "1.8.0_492"`

看项目，因为有个配置，要看父级的，在 ~/.m2/repository/com/example/platform/demo-parent/1.0.0/demo-parent-1.0.0.pom

```xml
<parent>
    <groupId>com.example.platform</groupId>
    <artifactId>demo-parent</artifactId>
    <version>5.1.0-SNAPSHOT</version>
</parent>
```

```xml
<maven.compiler.source>1.8</maven.compiler.source>
<maven.compiler.target>1.8</maven.compiler.target>
```

Spring Cloud？

##

.m2

模块拆分，biz 为业务逻辑 + client 提供 feign 供其他服务调用 + common 公共 DTO，service 启动入口

demo-openresty-gateway (API 网关)

## 常用平台

mysql

微服务

Eureka/Nacos

Feign

Apollo

定时任务

Kafka

## 可视化规格

> VISUAL_STRATEGY：架构图（Architecture）
> DIAGRAM_DESCRIPTION：围绕“本文主题”画出系统边界、核心组件、依赖方向、数据或控制流、外部服务和故障降级路径；权限边界与持久化位置必须明确。
