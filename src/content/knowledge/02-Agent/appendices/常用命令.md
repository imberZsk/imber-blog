# 附录：常用命令

> 目标：保留学习和运行 demo 时最常用的命令，减少来回搜索。

## Node demo

```bash
cd /Users/imber/Desktop/imber/Agent/agent-demo
npm test
npm start
```

## Python 虚拟环境

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m pytest
```

## 常见本地服务检查

```bash
curl http://localhost:5177/api/health
curl -X POST http://localhost:5177/api/agent/chat \
  -H 'Content-Type: application/json' \
  -d '{"message":"报销流程是什么？","sessionId":"demo"}'
```

## Git 检查

```bash
git status --short
git diff --stat
```

## 学习建议

每个项目 README 至少要写清楚三条命令：安装、启动、测试。没有这三条，demo 就还不算可交付。
