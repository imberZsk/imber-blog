# 面试题（04） - Skill、MCP、Hooks 与工具调用（第 31～40 题）

> 读完后，你应能完成以下任务：
> - 绘制“面试题（04） - Skill、MCP、Hooks 与工具调用（第 31～40 题） / 回答标准”的关键对象与数据流，解释“原理题：说清机制、输入输出和相近概念的边界。”，并用源码位置、日志或 Trace 标注证据。
> - 为“面试题（04） - Skill、MCP、Hooks 与工具调用（第 31～40 题） / 第31题：Skill 和 Prompt 模板的区别是什么？”设计正常与异常输入，验证“核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。”，输出首个偏差位置与回归测试结果。
> - 实现“面试题（04） - Skill、MCP、Hooks 与工具调用（第 31～40 题） / 第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？”的最小代码或配置，检验“Tool 是动作，Resource 是数据，Prompt 是模板；”，输出命令、结果与 Diff，并说明不适用边界。

<!-- article-progressive-block:start -->
# 一、先建立全局：Skill、MCP、Hooks 与工具调用（第 31～40 题） 是什么？

理解“Skill、MCP、Hooks 与工具调用（第 31～40 题）”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的第一个核心判断是：原理题：说清机制、输入输出和相近概念的边界。。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | 回答标准 | 原理题：说清机制、输入输出和相近概念的边界。 |
| 2 | 第31题：Skill 和 Prompt 模板的区别是什么？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 |
| 3 | 第32题：Skill 的 description 为什么会影响触发准确率？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 |
| 4 | 第33题：渐进式披露怎样降低 Token 消耗？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 |
| 5 | 第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？ | Tool 是动作，Resource 是数据，Prompt 是模板； |
| 6 | 第35题：MCP 的 Tool、Resource 和 Prompt 有什么区别？ | Tool 是动作，Resource 是数据，Prompt 是模板； |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["回答标准"] --> S2
  S2["第31题：Skill 和 Prompt 模板的区别是什么？"] --> S3
  S3["第32题：Skill 的 description 为什么会影响触发准确率？"] --> S4
  S4["第33题：渐进式披露怎样降低 Token 消耗？"] --> S5
  S5["第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“Skill、MCP、Hooks 与工具调用（第 31～40 题）”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的对象和顺序已经明确后，再看可观察的失败：文本直通执行、状态不可重放或重试重复写入。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、回答标准

- **原理题**：说清机制、输入输出和相近概念的边界。
- **选型题**：给出约束、比较维度、不适用场景和最终依据。

# 三、第31题：Skill 和 Prompt 模板的区别是什么？

**核心答案：** Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
description 同时写适用条件和反例；
先加载短元数据，命中后再按需读取正文与资源，才能降低 Token 和误触发。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

**工程实践：** 每个提交都应可独立解释和回退；
兼容性迁移要先扩展、再切流、最后清理旧路径。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 四、第32题：Skill 的 description 为什么会影响触发准确率？

**核心答案：** Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
description 同时写适用条件和反例；
先加载短元数据，命中后再按需读取正文与资源，才能降低 Token 和误触发。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

并保存足够证据供复现和回滚。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 五、第33题：渐进式披露怎样降低 Token 消耗？

**核心答案：** Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
description 同时写适用条件和反例；
先加载短元数据，命中后再按需读取正文与资源，才能降低 Token 和误触发。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

**工程实践：** 降级策略要写清质量底线，
例如缩小候选集、切换模型或转人工，
而不是静默返回低质量结果。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 六、第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？

**核心答案：** MCP 标准化 Host 与 Server 的能力发现和调用。
Tool 是动作，Resource 是数据，Prompt 是模板；
stdio 适合本地子进程，Streamable HTTP 适合远程服务。
Schema 不代替身份和资源授权。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

并保存足够证据供复现和回滚。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 七、第35题：MCP 的 Tool、Resource 和 Prompt 有什么区别？

**核心答案：** MCP 标准化 Host 与 Server 的能力发现和调用。
Tool 是动作，Resource 是数据，Prompt 是模板；
stdio 适合本地子进程，Streamable HTTP 适合远程服务。
Schema 不代替身份和资源授权。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

**工程实践：** 每个提交都应可独立解释和回退；
兼容性迁移要先扩展、再切流、最后清理旧路径。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 八、第36题：stdio 与 Streamable HTTP 应该怎样选？

**核心答案：** MCP 标准化 Host 与 Server 的能力发现和调用。
Tool 是动作，Resource 是数据，Prompt 是模板；
stdio 适合本地子进程，Streamable HTTP 适合远程服务。
Schema 不代替身份和资源授权。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

并保存足够证据供复现和回滚。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 九、第37题：MCP 能力发现和参数 Schema 为什么仍不能代替授权？

**核心答案：** MCP 标准化 Host 与 Server 的能力发现和调用。
Tool 是动作，Resource 是数据，Prompt 是模板；
stdio 适合本地子进程，Streamable HTTP 适合远程服务。
Schema 不代替身份和资源授权。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。
协议边界需要版本号、兼容策略和未知字段处理，避免客户端与服务端独立升级后静默误解。

**工程实践：** 错误要返回机器可判断的类别和可安全展示的信息，
不能把堆栈、密钥或内部路径直接暴露。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 十、第38题：Hooks 适合做哪些确定性校验，哪些逻辑不应放进 Hook？

**核心答案：** Hook 用于参数校验、权限阻断、格式化和审计等确定性策略，
并设置超时、失败策略和递归深度。
外部 Server 描述与结果都按不可信输入处理，高风险工具调用需要最小权限和二次确认。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

并保存足够证据供复现和回滚。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 十一、第39题：如何防止 Hook 递归触发、超时和阻塞主流程？

**核心答案：** Hook 用于参数校验、权限阻断、格式化和审计等确定性策略，
并设置超时、失败策略和递归深度。
外部 Server 描述与结果都按不可信输入处理，高风险工具调用需要最小权限和二次确认。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。

并保存足够证据供复现和回滚。
面试时还应补充一个失败案例和对应的止损或回滚办法。

# 十二、第40题：如何防御恶意 MCP Server、Tool Poisoning 和数据外传？

**核心答案：** Hook 用于参数校验、权限阻断、格式化和审计等确定性策略，
并设置超时、失败策略和递归深度。
外部 Server 描述与结果都按不可信输入处理，高风险工具调用需要最小权限和二次确认。

**判断依据：** Skill 解决怎么做，
MCP 解决怎样连接，
Hook 解决何时执行确定性策略；
协议发现不等于业务授权。
默认拒绝比默认放行更适合 Agent；
新增工具、目录或网络域名时要单独评审权限。

**工程实践：** 验收同时覆盖越权、提示词注入、密钥泄露和审计可追溯，不能只测正常路径。
面试时还应补充一个失败案例和对应的止损或回滚办法。

<!-- article-progressive-block:start -->
# 十三、动手验证：先跑通 Skill、MCP、Hooks 与工具调用（第 31～40 题），再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“Skill、MCP、Hooks 与工具调用（第 31～40 题）”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 13.1 基线与候选只允许一个变量不同

验证“Skill、MCP、Hooks 与工具调用（第 31～40 题）”时，先固定工具 Schema、身份、畸形参数、超时和重复请求。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“Skill、MCP、Hooks 与工具调用（第 31～40 题）”时，动作是：回放决策到执行链路，覆盖失败、重试、暂停和恢复。原始结果不能只保留截图或汇总分数，必须同步保存：模型提议、校验、授权、幂等键、状态迁移、Trace，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 工具 Schema、身份、畸形参数、超时和重复请求 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 通过阈值 | 模型只提议；执行受代码约束；失败不重复副作用 |
| 立即停止 | 文本直通执行、状态不可重放或重试重复写入 |

## 13.2 执行前先排除不可比较条件

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的当前环境重复运行。
- 候选只改变一个与“Skill、MCP、Hooks 与工具调用（第 31～40 题）”结论直接相关的条件。
- “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 13.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“Skill、MCP、Hooks 与工具调用（第 31～40 题）”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 工具 Schema、身份、畸形参数、超时和重复请求 |
| 过程可回放 | 回放决策到执行链路，覆盖失败、重试、暂停和恢复 |
| 结果可审计 | 模型提议、校验、授权、幂等键、状态迁移、Trace |

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的一次合格基线对照按以下顺序执行：

1. 保存“Skill、MCP、Hooks 与工具调用（第 31～40 题）”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“Skill、MCP、Hooks 与工具调用（第 31～40 题）”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“Skill、MCP、Hooks 与工具调用（第 31～40 题）”：回放决策到执行链路，覆盖失败、重试、暂停和恢复。
4. 为“Skill、MCP、Hooks 与工具调用（第 31～40 题）”保存：模型提议、校验、授权、幂等键、状态迁移、Trace。
5. 使用“Skill、MCP、Hooks 与工具调用（第 31～40 题）”预登记条件判断：模型只提议；执行受代码约束；失败不重复副作用。
6. 如果“Skill、MCP、Hooks 与工具调用（第 31～40 题）”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 十四、用一张矩阵验证 Skill、MCP、Hooks 与工具调用（第 31～40 题） 的关键结论

矩阵按正文顺序列出“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| 回答标准 | 原理题：说清机制、输入输出和相近概念的边界。 | 只改变与“回答标准”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 第31题：Skill 和 Prompt 模板的区别是什么？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 | 只改变与“第31题：Skill 和 Prompt 模板的区别是什么？”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 第32题：Skill 的 description 为什么会影响触发准确率？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 | 只改变与“第32题：Skill 的 description 为什么会影响触发准确率？”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 第33题：渐进式披露怎样降低 Token 消耗？ | 核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。 | 只改变与“第33题：渐进式披露怎样降低 Token 消耗？”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？ | Tool 是动作，Resource 是数据，Prompt 是模板； | 只改变与“第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 第35题：MCP 的 Tool、Resource 和 Prompt 有什么区别？ | Tool 是动作，Resource 是数据，Prompt 是模板； | 只改变与“第35题：MCP 的 Tool、Resource 和 Prompt 有什么区别？”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |

## 14.1 记录本次实际实验

下面的记录用于“Skill、MCP、Hooks 与工具调用（第 31～40 题）”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "Skill、MCP、Hooks 与工具调用（第 31～40 题）"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "回放决策到执行链路，覆盖失败、重试、暂停和恢复"
evidence: "模型提议、校验、授权、幂等键、状态迁移、Trace"
pass_when: "模型只提议；执行受代码约束；失败不重复副作用"
stop_when: "文本直通执行、状态不可重放或重试重复写入"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 14.2 边界实验必须证明能够停止和恢复

成功路径只能证明“Skill、MCP、Hooks 与工具调用（第 31～40 题）”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：文本直通执行、状态不可重放或重试重复写入，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 模型提议、校验、授权、幂等键、状态迁移、Trace | 模型只提议；执行受代码约束；失败不重复副作用 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：文本直通执行、状态不可重放或重试重复写入 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：关闭副作用入口，恢复检查点，补充失败契约测试 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“Skill、MCP、Hooks 与工具调用（第 31～40 题）”，第一步是：关闭副作用入口，恢复检查点，补充失败契约测试。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 十五、Skill、MCP、Hooks 与工具调用（第 31～40 题） 的结果解释

解释“Skill、MCP、Hooks 与工具调用（第 31～40 题）”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 文本直通执行、状态不可重放或重试重复写入 | 先执行：关闭副作用入口，恢复检查点，补充失败契约测试 |
| 异常链路无法恢复 | 文本直通执行、状态不可重放或重试重复写入 | 先执行：关闭副作用入口，恢复检查点，补充失败契约测试 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”只有同时满足“模型只提议；执行受代码约束；失败不重复副作用”，并且没有出现“文本直通执行、状态不可重放或重试重复写入”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“Skill、MCP、Hooks 与工具调用（第 31～40 题）”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“Skill、MCP、Hooks 与工具调用（第 31～40 题）”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十六、Skill、MCP、Hooks 与工具调用（第 31～40 题） 的发布判断

发布判断需要把“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的基线与候选只存在一个计划内变量。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的原始输出、中间状态和失败现场已经保留。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”的停止条件、负责人和回滚入口已经演练。
- [ ] “Skill、MCP、Hooks 与工具调用（第 31～40 题）”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“Skill、MCP、Hooks 与工具调用（第 31～40 题）”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十七、总结

- **第31题：Skill 和 Prompt 模板的区别是什么？**：核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
- **第32题：Skill 的 description 为什么会影响触发准确率？**：核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
- **第33题：渐进式披露怎样降低 Token 消耗？**：核心答案： Skill 是带触发描述、步骤、资源和脚本的可复用工作流。
- **第34题：MCP 解决什么问题，什么时候没有必要使用 MCP？**：Tool 是动作，Resource 是数据，Prompt 是模板；
- **第35题：MCP 的 Tool、Resource 和 Prompt 有什么区别？**：Tool 是动作，Resource 是数据，Prompt 是模板；

## 参考资料

- [AI Engineering Interview Questions](https://github.com/amitshekhariitbhu/ai-engineering-interview-questions)
- [AI Engineering Field Guide](https://github.com/alexeygrigorev/ai-engineering-field-guide)
- [AI Agent Engineer Handbook](https://github.com/harrisliangsu/ai-agent-engineer-handbook)
- [AI Engineer Interview QA](https://github.com/Nareshedagotti/AI-Engineer-Interview-QA)
