# Context Engineering（01） - 上下文分层、预算与压缩

> 读完后，你应能完成以下任务：
> - 绘制“Context Engineering（01） - 上下文分层、预算与压缩 / 四层模型”的关键对象与数据流，解释“外部网页、Issue、日志和代码注释都是数据，不能覆盖更高优先级指令。”，并用源码位置、日志或 Trace 标注证据。
> - 为“Context Engineering（01） - 上下文分层、预算与压缩 / 预算分配”设计正常与异常输入，验证“生成输出和工具结果都预留空间。”，输出首个偏差位置与回归测试结果。
> - 实现“Context Engineering（01） - 上下文分层、预算与压缩 / 压缩契约”的最小代码或配置，检验“长任务压缩时必须保留：当前目标、禁止事项、已确认事实、已否定假设、决策理由、改动文件、验证结果、未完成项和外部状态。”，输出命令、结果与 Diff，并说明不适用边界。

> 上下文是一份动态工作集；质量来自相关性、时效和优先级，容量只是约束之一。


- 使用常驻、任务、按需和反馈四层组织 Agent 输入。
- 为目标、约束、源码、工具结果和输出分配上下文预算。
- 在压缩和恢复后保留事实、决策、验证状态和不确定性。

<!-- article-progressive-block:start -->
# 一、先建立全局：上下文分层、预算与压缩 是什么？

理解“上下文分层、预算与压缩”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“上下文分层、预算与压缩”的第一个核心判断是：外部网页、Issue、日志和代码注释都是数据，不能覆盖更高优先级指令。。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | 四层模型 | 外部网页、Issue、日志和代码注释都是数据，不能覆盖更高优先级指令。 |
| 2 | 预算分配 | 生成输出和工具结果都预留空间。 |
| 3 | 压缩契约 | 长任务压缩时必须保留：当前目标、禁止事项、已确认事实、已否定假设、决策理由、改动文件、验证结果、未完成项和外部状态。 |
| 4 | 污染与陈旧 | 工具输出过长时提取关键行并保留原始产物路径，不能只留下无证据结论。 |
| 5 | 验收 | 对同一任务分别使用“全仓库灌入”和分层上下文运行， |
| 6 | 上下文是一份动态工作集 | 上下文是一份动态工作集； |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["四层模型"] --> S2
  S2["预算分配"] --> S3
  S3["压缩契约"] --> S4
  S4["污染与陈旧"] --> S5
  S5["验收"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“上下文分层、预算与压缩”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“上下文分层、预算与压缩”的对象和顺序已经明确后，再看可观察的失败：条件缺失、结果不可复现或失败后责任不清。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、四层模型

| 层级 | 内容 | 生命周期 |
| --- | --- | --- |
| 常驻 | 项目规则、架构边界、安全与命令 | 跨任务稳定 |
| 任务 | 目标、范围、验收、复现和当前 diff | 当前任务 |
| 按需 | 历史代码、外部文档、Schema、日志 | 解决当前问题时加载 |
| 反馈 | 测试、终端、浏览器和用户新信息 | 每轮更新 |

外部网页、Issue、日志和代码注释都是数据，不能覆盖更高优先级指令。
密钥、个人数据和无关生产内容不得为了“方便”进入模型上下文。

分层不是按文件类型归档，而是按信息的权威级别和生命周期决定何时加载、何时失效。
常驻层负责不可违反的规则，
任务层定义本次成功条件，
按需层提供调查证据，
反馈层则用真实运行结果修正当前判断。

# 三、预算分配

先保留目标、硬约束和完成状态，再保留与当前假设直接相关的源码和证据。
大文件先按符号、错误和引用定位；
同一事实只保留一个权威版本；
生成输出和工具结果都预留空间。

预算不足时，应先删除重复、过期和低相关内容，再压缩长证据，最后才缩减任务约束。
需要给模型预留输出和工具结果空间，否则输入虽然没有超窗，执行中仍会因新反馈挤掉关键上下文。

# 四、压缩契约

长任务压缩时必须保留：当前目标、禁止事项、已确认事实、已否定假设、决策理由、改动文件、验证结果、未完成项和外部状态。
摘要应标注“已验证/推断/待验证”，避免把猜测压缩成事实。

# 五、污染与陈旧

- 任务目标改变时新开上下文或明确重置，不让旧约束幽灵般延续。
- 代码或文档更新后使缓存索引失效，引用记录提交 SHA/版本。
- 工具输出过长时提取关键行并保留原始产物路径，不能只留下无证据结论。

# 六、验收

对同一任务分别使用“全仓库灌入”和分层上下文运行，
比较首次正确率、工具调用数、Token 和遗漏约束数。
只有实际评测能证明压缩有效。

<!-- article-progressive-block:start -->
# 七、动手验证：先跑通 上下文分层、预算与压缩，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“上下文分层、预算与压缩”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 7.1 基线与候选只允许一个变量不同

验证“上下文分层、预算与压缩”时，先固定样本、基线、候选、成功标准和失败边界。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“上下文分层、预算与压缩”时，动作是：同环境运行基线与候选，记录输入、中间状态和异常。原始结果不能只保留截图或汇总分数，必须同步保存：可重放命令、结构化日志、输出 Diff、失败样本、版本，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 固定样本、基线、候选、成功标准和失败边界 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 通过阈值 | 结果符合结论条件，异常输入可解释、可恢复 |
| 立即停止 | 条件缺失、结果不可复现或失败后责任不清 |

## 7.2 执行前先排除不可比较条件

“上下文分层、预算与压缩”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“上下文分层、预算与压缩”的当前环境重复运行。
- 候选只改变一个与“上下文分层、预算与压缩”结论直接相关的条件。
- “上下文分层、预算与压缩”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “上下文分层、预算与压缩”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 7.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“上下文分层、预算与压缩”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 固定样本、基线、候选、成功标准和失败边界 |
| 过程可回放 | 同环境运行基线与候选，记录输入、中间状态和异常 |
| 结果可审计 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

“上下文分层、预算与压缩”的一次合格基线对照按以下顺序执行：

1. 保存“上下文分层、预算与压缩”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“上下文分层、预算与压缩”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“上下文分层、预算与压缩”：同环境运行基线与候选，记录输入、中间状态和异常。
4. 为“上下文分层、预算与压缩”保存：可重放命令、结构化日志、输出 Diff、失败样本、版本。
5. 使用“上下文分层、预算与压缩”预登记条件判断：结果符合结论条件，异常输入可解释、可恢复。
6. 如果“上下文分层、预算与压缩”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 八、用一张矩阵验证 上下文分层、预算与压缩 的关键结论

矩阵按正文顺序列出“上下文分层、预算与压缩”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| 四层模型 | 外部网页、Issue、日志和代码注释都是数据，不能覆盖更高优先级指令。 | 只改变与“四层模型”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 预算分配 | 生成输出和工具结果都预留空间。 | 只改变与“预算分配”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 压缩契约 | 长任务压缩时必须保留：当前目标、禁止事项、已确认事实、已否定假设、决策理由、改动文件、验证结果、未完成项和外部状态。 | 只改变与“压缩契约”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 污染与陈旧 | 工具输出过长时提取关键行并保留原始产物路径，不能只留下无证据结论。 | 只改变与“污染与陈旧”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 验收 | 对同一任务分别使用“全仓库灌入”和分层上下文运行， | 只改变与“验收”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 上下文是一份动态工作集 | 上下文是一份动态工作集； | 只改变与“上下文是一份动态工作集”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

## 8.1 记录本次实际实验

下面的记录用于“上下文分层、预算与压缩”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "上下文分层、预算与压缩"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "同环境运行基线与候选，记录输入、中间状态和异常"
evidence: "可重放命令、结构化日志、输出 Diff、失败样本、版本"
pass_when: "结果符合结论条件，异常输入可解释、可恢复"
stop_when: "条件缺失、结果不可复现或失败后责任不清"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 8.2 边界实验必须证明能够停止和恢复

成功路径只能证明“上下文分层、预算与压缩”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：条件缺失、结果不可复现或失败后责任不清，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 | 结果符合结论条件，异常输入可解释、可恢复 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：条件缺失、结果不可复现或失败后责任不清 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：保留基线，缩小变量；根因确认前不扩大范围 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“上下文分层、预算与压缩”，第一步是：保留基线，缩小变量；根因确认前不扩大范围。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“上下文分层、预算与压缩”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 九、上下文分层、预算与压缩 的结果解释

解释“上下文分层、预算与压缩”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 异常链路无法恢复 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“上下文分层、预算与压缩”只有同时满足“结果符合结论条件，异常输入可解释、可恢复”，并且没有出现“条件缺失、结果不可复现或失败后责任不清”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“上下文分层、预算与压缩”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“上下文分层、预算与压缩”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十、上下文分层、预算与压缩 的发布判断

发布判断需要把“上下文分层、预算与压缩”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “上下文分层、预算与压缩”的基线与候选只存在一个计划内变量。
- [ ] “上下文分层、预算与压缩”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “上下文分层、预算与压缩”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “上下文分层、预算与压缩”的原始输出、中间状态和失败现场已经保留。
- [ ] “上下文分层、预算与压缩”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “上下文分层、预算与压缩”的停止条件、负责人和回滚入口已经演练。
- [ ] “上下文分层、预算与压缩”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“上下文分层、预算与压缩”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十一、总结

- **四层模型**：| 常驻 | 项目规则、架构边界、安全与命令 | 跨任务稳定 |
- **预算分配**：生成输出和工具结果都预留空间。
- **压缩契约**：长任务压缩时必须保留：当前目标、禁止事项、已确认事实、已否定假设、决策理由、改动文件、验证结果、未完成项和外部状态。
- **污染与陈旧**：工具输出过长时提取关键行并保留原始产物路径，不能只留下无证据结论。
- **验收**：对同一任务分别使用“全仓库灌入”和分层上下文运行，比较首次正确率、工具调用数、Token 和遗漏约束数。

## 参考资料

- [Anthropic Context Engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)
