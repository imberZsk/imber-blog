# 工程化工作流（07） - grill-me：把模糊需求问清楚

> 读完后，你应能完成以下任务：
> - 绘制“工程化工作流（07） - grill-me：把模糊需求问清楚 / 为什么普通“请先问我”仍会失败”的关键对象与数据流，解释“只讨论正常路径，不问权限、失败、回滚和完成条件。”，并用源码位置、日志或 Trace 标注证据。
> - 为“工程化工作流（07） - grill-me：把模糊需求问清楚 / 一轮高质量访谈”设计正常与异常输入，验证“范围确定后，第二轮才问触发来源、日志权限、修改目录、重试预算、超时和回滚。”，输出首个偏差位置与回归测试结果。
> - 实现“工程化工作流（07） - grill-me：把模糊需求问清楚 / 交付物不是聊天记录”的最小代码或配置，检验“访谈结束时至少输出六项：问题陈述、目标与非目标、已确认决策、事实与来源、仍未解决的问题、可验证验收。”，输出命令、结果与 Diff，并说明不适用边界。

> grill-me 不是让 Agent “多问几个问题”，而是把设计拆成有依赖的决策树：能调查的事实由 Agent 调查，只有真实取舍才交给用户。


## 核心知识清单

- 决策树与问题前沿 Frontier
- 事实调查和用户决策的职责分离
- 每轮问题的依赖顺序与推荐答案
- 目标、非目标、约束、失败和验收
- 共享理解确认与访谈停止条件
- 从访谈记录到规范系统的交接

<!-- article-progressive-block:start -->
# 一、先建立全局：grill-me：把模糊需求问清楚 是什么？

理解“grill-me：把模糊需求问清楚”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“grill-me：把模糊需求问清楚”的第一个核心判断是：只讨论正常路径，不问权限、失败、回滚和完成条件。。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | 为什么普通“请先问我”仍会失败 | 只讨论正常路径，不问权限、失败、回滚和完成条件。 |
| 2 | 一轮高质量访谈 | 范围确定后，第二轮才问触发来源、日志权限、修改目录、重试预算、超时和回滚。 |
| 3 | 交付物不是聊天记录 | 访谈结束时至少输出六项：问题陈述、目标与非目标、已确认决策、事实与来源、仍未解决的问题、可验证验收。 |
| 4 | 不适用和常见坑 | 推荐答案应暴露取舍，不应暗中替用户决定业务目标。 |
| 5 | 决策树与问题前沿 Frontier | grill-me 不是让 Agent “多问几个问题”，而是把设计拆成有依赖的决策树：能调查的事实由 Agent 调查，只有真实取舍才交给用户。 |
| 6 | 每轮问题的依赖顺序与推荐答案 | 每个问题都给出推荐答案和理由，用户可以接受、修改或拒绝，而不是从空白开始设计。 |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["为什么普通“请先问我”仍会失败"] --> S2
  S2["一轮高质量访谈"] --> S3
  S3["交付物不是聊天记录"] --> S4
  S4["不适用和常见坑"] --> S5
  S5["决策树与问题前沿 Frontier"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“grill-me：把模糊需求问清楚”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“grill-me：把模糊需求问清楚”的对象和顺序已经明确后，再看可观察的失败：文本直通执行、状态不可重放或重试重复写入。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、为什么普通“请先问我”仍会失败

无结构访谈常出现三类浪费：一次抛出 30 个互相依赖的问题；把仓库结构、框架版本等可查事实推给用户；只讨论正常路径，不问权限、失败、回滚和完成条件。结果是问题很多，决策仍然缺失。

grill-me 实际调用底层 `grilling` Skill。它先画出决策之间的依赖，当前置决策已经解决后，相关问题才进入本轮 **Frontier**。每个问题都给出推荐答案和理由，用户可以接受、修改或拒绝，而不是从空白开始设计。

# 三、一轮高质量访谈

以“自动修复失败 CI”为例，第一轮只问目标与风险边界：

```text
Q1 目标范围：只诊断，还是允许修改代码并提交 PR？
推荐：允许修改，但禁止直接推送 main；每次运行只创建一个待审查 PR。

Q2 成功标准：CI 变绿就算完成吗？
推荐：原失败消失、相关回归通过、没有新增失败，并附根因与 diff 摘要。

Q3 高风险动作：依赖升级、迁移和密钥变更怎么处理？
推荐：只生成方案并暂停，必须人工批准后执行。
```

范围确定后，第二轮才问触发来源、日志权限、修改目录、重试预算、超时和回滚。Agent 应先读取现有 CI、分支保护和测试脚本，不把“项目现在怎么配置”当作用户问答题。

# 四、交付物不是聊天记录

访谈结束时至少输出六项：问题陈述、目标与非目标、已确认决策、事实与来源、仍未解决的问题、可验证验收。对每个决策记录“为什么”，否则下一位 Agent 只看到结论，遇到新约束时无法判断是否应该调整。

停止条件是 Frontier 为空，且用户确认双方已形成共享理解。不能用“已经问了三轮”作为停止条件，也不能在用户确认前直接实现。随后将产物交给 OpenSpec 的 proposal/change 或 Spec Kit 的 specification，而不是让访谈内容只留在对话上下文里。

# 五、不适用和常见坑

- 拼写修复、明确的小范围改动不值得启动完整访谈。
- 推荐答案应暴露取舍，不应暗中替用户决定业务目标。
- 问题必须具体到可决策；“还有别的要求吗”无法关闭决策树。
- 用户改变上游决定后，要重新计算 Frontier，不能沿用已经失效的下游答案。

## 学完验收

- 能把一个需求拆成事实节点、决策节点及其依赖。
- 能解释为什么某个问题现在问，而另一个问题必须晚一轮。
- 访谈产物可被另一位 Agent 直接转成规格，且无需重新猜目标和边界。

<!-- article-progressive-block:start -->
# 六、动手验证：先跑通 grill-me：把模糊需求问清楚，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“grill-me：把模糊需求问清楚”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 6.1 基线与候选只允许一个变量不同

验证“grill-me：把模糊需求问清楚”时，先固定工具 Schema、身份、畸形参数、超时和重复请求。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“grill-me：把模糊需求问清楚”时，动作是：回放决策到执行链路，覆盖失败、重试、暂停和恢复。原始结果不能只保留截图或汇总分数，必须同步保存：模型提议、校验、授权、幂等键、状态迁移、Trace，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 工具 Schema、身份、畸形参数、超时和重复请求 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 通过阈值 | 模型只提议；执行受代码约束；失败不重复副作用 |
| 立即停止 | 文本直通执行、状态不可重放或重试重复写入 |

## 6.2 执行前先排除不可比较条件

“grill-me：把模糊需求问清楚”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“grill-me：把模糊需求问清楚”的当前环境重复运行。
- 候选只改变一个与“grill-me：把模糊需求问清楚”结论直接相关的条件。
- “grill-me：把模糊需求问清楚”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “grill-me：把模糊需求问清楚”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 6.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“grill-me：把模糊需求问清楚”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 工具 Schema、身份、畸形参数、超时和重复请求 |
| 过程可回放 | 回放决策到执行链路，覆盖失败、重试、暂停和恢复 |
| 结果可审计 | 模型提议、校验、授权、幂等键、状态迁移、Trace |

“grill-me：把模糊需求问清楚”的一次合格基线对照按以下顺序执行：

1. 保存“grill-me：把模糊需求问清楚”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“grill-me：把模糊需求问清楚”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“grill-me：把模糊需求问清楚”：回放决策到执行链路，覆盖失败、重试、暂停和恢复。
4. 为“grill-me：把模糊需求问清楚”保存：模型提议、校验、授权、幂等键、状态迁移、Trace。
5. 使用“grill-me：把模糊需求问清楚”预登记条件判断：模型只提议；执行受代码约束；失败不重复副作用。
6. 如果“grill-me：把模糊需求问清楚”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 七、用一张矩阵验证 grill-me：把模糊需求问清楚 的关键结论

矩阵按正文顺序列出“grill-me：把模糊需求问清楚”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| 为什么普通“请先问我”仍会失败 | 只讨论正常路径，不问权限、失败、回滚和完成条件。 | 只改变与“为什么普通“请先问我”仍会失败”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 一轮高质量访谈 | 范围确定后，第二轮才问触发来源、日志权限、修改目录、重试预算、超时和回滚。 | 只改变与“一轮高质量访谈”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 交付物不是聊天记录 | 访谈结束时至少输出六项：问题陈述、目标与非目标、已确认决策、事实与来源、仍未解决的问题、可验证验收。 | 只改变与“交付物不是聊天记录”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 不适用和常见坑 | 推荐答案应暴露取舍，不应暗中替用户决定业务目标。 | 只改变与“不适用和常见坑”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 决策树与问题前沿 Frontier | grill-me 不是让 Agent “多问几个问题”，而是把设计拆成有依赖的决策树：能调查的事实由 Agent 调查，只有真实取舍才交给用户。 | 只改变与“决策树与问题前沿 Frontier”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |
| 每轮问题的依赖顺序与推荐答案 | 每个问题都给出推荐答案和理由，用户可以接受、修改或拒绝，而不是从空白开始设计。 | 只改变与“每轮问题的依赖顺序与推荐答案”相关的条件 | 模型提议、校验、授权、幂等键、状态迁移、Trace |

## 7.1 记录本次实际实验

下面的记录用于“grill-me：把模糊需求问清楚”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "grill-me：把模糊需求问清楚"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "回放决策到执行链路，覆盖失败、重试、暂停和恢复"
evidence: "模型提议、校验、授权、幂等键、状态迁移、Trace"
pass_when: "模型只提议；执行受代码约束；失败不重复副作用"
stop_when: "文本直通执行、状态不可重放或重试重复写入"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 7.2 边界实验必须证明能够停止和恢复

成功路径只能证明“grill-me：把模糊需求问清楚”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：文本直通执行、状态不可重放或重试重复写入，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 模型提议、校验、授权、幂等键、状态迁移、Trace | 模型只提议；执行受代码约束；失败不重复副作用 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：文本直通执行、状态不可重放或重试重复写入 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：关闭副作用入口，恢复检查点，补充失败契约测试 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“grill-me：把模糊需求问清楚”，第一步是：关闭副作用入口，恢复检查点，补充失败契约测试。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“grill-me：把模糊需求问清楚”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 八、grill-me：把模糊需求问清楚 的结果解释

解释“grill-me：把模糊需求问清楚”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 文本直通执行、状态不可重放或重试重复写入 | 先执行：关闭副作用入口，恢复检查点，补充失败契约测试 |
| 异常链路无法恢复 | 文本直通执行、状态不可重放或重试重复写入 | 先执行：关闭副作用入口，恢复检查点，补充失败契约测试 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“grill-me：把模糊需求问清楚”只有同时满足“模型只提议；执行受代码约束；失败不重复副作用”，并且没有出现“文本直通执行、状态不可重放或重试重复写入”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“grill-me：把模糊需求问清楚”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“grill-me：把模糊需求问清楚”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 九、grill-me：把模糊需求问清楚 的发布判断

发布判断需要把“grill-me：把模糊需求问清楚”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “grill-me：把模糊需求问清楚”的基线与候选只存在一个计划内变量。
- [ ] “grill-me：把模糊需求问清楚”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “grill-me：把模糊需求问清楚”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “grill-me：把模糊需求问清楚”的原始输出、中间状态和失败现场已经保留。
- [ ] “grill-me：把模糊需求问清楚”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “grill-me：把模糊需求问清楚”的停止条件、负责人和回滚入口已经演练。
- [ ] “grill-me：把模糊需求问清楚”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“grill-me：把模糊需求问清楚”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十、总结

- **为什么普通“请先问我”仍会失败**：只讨论正常路径，不问权限、失败、回滚和完成条件。
- **一轮高质量访谈**：以“自动修复失败 CI”为例，第一轮只问目标与风险边界：
- **交付物不是聊天记录**：访谈结束时至少输出六项：问题陈述、目标与非目标、已确认决策、事实与来源、仍未解决的问题、可验证验收。
- **不适用和常见坑**：推荐答案应暴露取舍，不应暗中替用户决定业务目标。

## 参考资料

- [grill-me 源码](https://github.com/mattpocock/skills/tree/main/skills/productivity/grill-me)
- [grilling 访谈纪律](https://github.com/mattpocock/skills/tree/main/skills/productivity/grilling)
- [Agent Skills 规范](https://agentskills.io/specification)
