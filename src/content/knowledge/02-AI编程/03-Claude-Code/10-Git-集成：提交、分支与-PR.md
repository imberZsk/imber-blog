# Claude Code（9）- Git 集成：提交、分支与 PR

> 本章目标：让 Claude Code 帮你写提交信息、管分支、建 PR，并理解它的安全边界。
> 学完你能：用自然语言完成日常 Git 操作，知道哪些破坏性操作它不会擅自做。

---

# 一、它能帮你做的 Git 活

日常 Git 操作大多可以用一句话交给它：

```
# 总结改动并提交
把我这次的改动总结一下，写一条规范的 commit message 并提交

# 开分支干活
基于 main 开一个 feature/user-pagination 分支

# 建 PR
把当前分支推上去，创建一个 PR，标题简洁、描述里说明改了什么、怎么测的
```

它会先看 `git diff` / `git status` 搞清你改了啥，再生成贴切的提交信息或 PR 描述——**不是套模板，而是基于真实改动**。

---

# 二、写好 commit 和 PR 的小技巧

**提交信息**：让它基于实际 diff 总结，比你手写「fix bug」之类的强。可以要求遵循团队规范：
```
按 Conventional Commits 规范写这条 commit message
```

**PR 描述**：好的 PR 描述包含三块——**改了什么、怎么测的、有什么注意事项**。可以让它先生成再增强：
```
创建 PR
# 看完它的初稿后
在 PR 描述里补充这次安全相关改动的背景说明
```

> 💡 PR 标题保持简洁（70 字以内），细节放描述里。

---

# 三、重点：它的安全边界（不会擅自干的事）

这是本章最该记牢的。Claude Code 在 Git 上**默认偏保守**，因为有些操作不可逆、影响协作：

- **不会直接推到 main/master**：默认推到新分支，除非你明确要求。
- **破坏性操作默认不做**：`push --force`、`reset --hard`、`clean -f`、`branch -D` 这类会丢东西的，需要你**明确授权**。
- **提交要你发话**：它不会动不动就自作主张提交，通常等你说「提交吧」。
- **会提醒敏感文件**：发现 `.env`、密钥文件之类，提交前会提示你。
- **不乱改 git 配置**：你的 `git config` 它不碰。

> 🔑 一句话：**它把「可能丢代码 / 影响别人」的操作的决定权留给你。** 这是保护，不是缺陷。

---

# 四、典型工作流：从改完到 PR

```
1. 我改完了用户分页功能，帮我看一下 git diff，确认没有遗漏。
2. 基于 main 开个分支 feature/user-pagination。
3. 把改动提交，commit message 按 Conventional Commits 写。
4. 推到远程并创建 PR，描述里写清功能点和测试方式。
```

一条龙下来，你只需审查它的 diff 和 PR 描述。

---

# 五、常见错误

**错误 1：让它「提交所有改动」却没先看 diff**
可能把不相关的改动、调试代码一起提交了。→ 先 `git diff` 审查，或让它分文件提交。

**错误 2：指望它 `--force` push 救场**
破坏性操作它默认不做，且你也不该随手用。真要用，想清楚后果再明确授权。

**错误 3：把密钥提交上去**
它会提醒，但最终是你点的确认。→ 留意它的敏感文件提示，配好 `.gitignore`。

**错误 4：在 main 上直接干**
养成开分支的习惯，让它「基于 main 开新分支」，别在主干上乱来。

---

# 六、最佳实践

1. **提交前看 diff**：让它总结改动，你扫一眼再放行。
2. **遵循团队提交规范**：明确告诉它用 Conventional Commits 之类。
3. **善用它的保守默认**：它不擅自推 main、不做破坏性操作，顺着这个安全习惯走。
4. **PR 描述三件套**：改了什么 / 怎么测 / 注意事项。
5. **敏感文件配好忽略**：别依赖事后提醒，`.gitignore` 先行。

---

# 七、总结

- 提交信息、分支、PR 都能用一句话交给它，且**基于真实 diff** 生成。
- 它的**安全边界**：不擅自推 main、破坏性操作要授权、提交要你发话、提醒敏感文件。
- 这些限制是保护你和团队，顺着用最稳。

进阶篇最后一章，我们学怎么把高频流程封装成 `/命令`。👉 `11-Slash命令与自定义命令.md`

<!-- knowledge-lab-merged -->

# 动手实践：Demo 09 · Git 工作流与安全边界演练

本 Demo 给你一份「Git 一条龙」对话脚本和一张「安全边界对照表」，帮你练习用自然语言管 Git，并记牢它默认不会擅自做的危险操作。

## 文件说明
- `git一条龙.md`：从改完代码到提 PR 的标准对话脚本。
- `安全边界对照.md`：哪些操作它默认会做 / 默认不做。

## 怎么用
1. 在一个**真实的 git 项目**里（不是这个 demo 目录），照 `git一条龙.md` 走一遍。
2. 对照 `安全边界对照.md`，体会它在破坏性操作上的保守默认。

## 实践目标

本 Lab 用最小输入验证“Demo 09 · Git 工作流与安全边界演练”的核心行为。先按上文命令运行基线，记录输入、关键中间状态和最终输出；再只修改一个参数或一个分支，比较结果差异。不要在第一次运行时同时更换依赖、模型和数据，否则失败后无法定位变量。

## 实践验收

1. 安装、启动或执行命令退出码为 0，输出与 README 描述一致。
2. 至少准备一个正常输入、一个边界输入和一个失败输入，并说明系统为什么得到该结果。
3. 涉及模型、检索或工具时，记录请求 ID、版本、候选或工具参数，保证结果可复现。
4. 涉及文件、网络或写操作时，验证路径、超时、权限和幂等边界；错误不能只打印后继续伪装成功。

## 常见问题

- 环境失败：先确认 Python/Node 版本、工作目录和依赖安装结果，再检查业务代码。
- 结果不稳定：固定样例、随机种子和配置版本，分别记录输入与输出。
- 示例能跑但无法解释：逐步打印或断言中间状态，直到能说清每个阶段的输入、处理和产物。

<!-- knowledge-practice-materials-merged -->

## 配套实践材料

以下材料已并入正文，便于阅读时直接对照和练习。

### `安全边界对照.md`

````markdown
# Git 安全边界对照表

| 操作 | 默认行为 |
| --- | --- |
| 推到 main/master | ❌ 不直接推，默认推新分支（除非你明确要求） |
| `git push --force` | ❌ 不擅自做，需明确授权 |
| `git reset --hard` | ❌ 不擅自做，需明确授权 |
| `git clean -f` | ❌ 不擅自做，需明确授权 |
| `git branch -D` | ❌ 不擅自做，需明确授权 |
| 提交改动 | ⏸ 通常等你发话才提交 |
| 遇到 .env / 密钥文件 | ⚠️ 提交前提醒你 |
| 改 git config | ❌ 不碰 |

> 口诀：可能丢代码 / 影响别人的操作，决定权留给你。这是保护，不是缺陷。
````

### `git一条龙.md`

````markdown
# Git 一条龙：从改完到 PR

> 在真实 git 项目里照着发给 Claude Code。

1. 帮我看一下 git diff，确认这次改动没有遗漏或多余的调试代码。
2. 基于 main 开一个分支 feature/xxx。
3. 把改动提交，commit message 按 Conventional Commits 规范写。
4. 推到远程并创建 PR，标题简洁，描述里写清：改了什么、怎么测的、注意事项。

## 小技巧
- 提交前一定先看 diff，别直接「提交所有改动」。
- 嫌它生成的 PR 描述单薄？追一句：「在描述里补充这次安全相关改动的背景」。
````

## 参考资料

- [Claude Code 文档](https://docs.anthropic.com/en/docs/claude-code/overview)
- [Claude Code 安全](https://docs.anthropic.com/en/docs/claude-code/security)
