# Demo 15 · /loop 命令速查与场景脚本

## 文件
- `loop速查.md`：三种用法 + 选型 + 可直接发的场景脚本。
- `loop.md.example`：放到项目 `.claude/loop.md` 的内置循环提示模板。

## 怎么用
1. 看 `loop速查.md`，挑一个场景在真实项目里发给 Claude。
2. 想让裸 `/loop` 跑自定义内容？把 `loop.md.example` 复制为项目根 `.claude/loop.md`。
