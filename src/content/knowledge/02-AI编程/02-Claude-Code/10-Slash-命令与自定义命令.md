# Claude Code（10） - Slash 命令与自定义命令

> 读完后，你应能完成以下任务：
> - 绘制“Claude Code（10） - Slash 命令与自定义命令 / 什么是 Slash 命令”的关键对象与数据流，解释“在对话里输入 / 开头的指令，”，并用源码位置、日志或 Trace 标注证据。
> - 为“Claude Code（10） - Slash 命令与自定义命令 / 常用内置命令”设计正常与异常输入，验证“💡 /clear 很重要：一个会话里堆了太多无关任务，上下文会变乱、变贵。”，输出首个偏差位置与回归测试结果。
> - 实现“Claude Code（10） - Slash 命令与自定义命令 / 重点：自定义命令——把重复流程封装起来”的最小代码或配置，检验“这才是 Slash 命令的威力所在。”，输出命令、结果与 Diff，并说明不适用边界。

> 本章目标：掌握内置斜杠命令，把常用流程封装成自定义命令。


---

<!-- article-progressive-block:start -->
# 一、先建立全局：Slash 命令与自定义命令 是什么？

理解“Slash 命令与自定义命令”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“Slash 命令与自定义命令”的第一个核心判断是：在对话里输入 / 开头的指令，。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | 什么是 Slash 命令 | 在对话里输入 / 开头的指令， |
| 2 | 常用内置命令 | 💡 /clear 很重要：一个会话里堆了太多无关任务，上下文会变乱、变贵。 |
| 3 | 重点：自定义命令——把重复流程封装起来 | 这才是 Slash 命令的威力所在。 |
| 4 | 项目级 vs 用户级命令 | 项目级：放项目里的 .claude/commands/，提交进 git，全团队共享（如团队统一的 review、release 流程）。 |
| 5 | 常见错误 | 错误 1：同一段长提示反复手打 |
| 6 | 最佳实践 | 复用即封装：一段提示用了几次，就做成命令。 -> 善用参数：用占位符让命令通用，别写死。 -> 分级归位：团队流程→项目级（共享），个人流程→用户级。 -> 命令名见名知意：/review、/fix-issue、/release，一看就懂。 |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["什么是 Slash 命令"] --> S2
  S2["常用内置命令"] --> S3
  S3["重点：自定义命令——把重复流程封装起来"] --> S4
  S4["项目级 vs 用户级命令"] --> S5
  S5["常见错误"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“Slash 命令与自定义命令”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“Slash 命令与自定义命令”的对象和顺序已经明确后，再看可观察的失败：条件缺失、结果不可复现或失败后责任不清。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、什么是 Slash 命令

在对话里输入 `/` 开头的指令，
就是 **Slash 命令（斜杠命令）**——一键触发某个功能或流程，
不用每次打一长段话。
输入 `/` 会弹出所有可用命令列表。

它分两类：**内置命令**（Claude Code 自带）和**自定义命令**（你自己造的）。

---

# 三、常用内置命令

记住这几个高频的，日常够用：

| 命令 | 作用 |
| --- | --- |
| `/help` | 看帮助和可用命令 |
| `/clear` | 清空当前对话上下文，开个干净的（换任务时常用） |
| `/agents` | 管理子代理（第 11 章） |
| `/exit` | 退出 |

> 💡 `/clear` 很重要：一个会话里堆了太多无关任务，上下文会变乱、变贵。换任务时 `/clear` 一下，相当于「翻篇」。（上下文治理见第 22 章。）

---

# 四、重点：自定义命令——把重复流程封装起来

这才是 Slash 命令的威力所在。
**某段提示词你反复在用，就该把它做成一个命令。
**

## 4.1 怎么做

自定义命令本质是一个 **Markdown 文件**，
放在 `.claude/commands/` 目录下。
文件名就是命令名。

例如，建一个 `.claude/commands/review.md`：

```markdown
审查当前的代码改动，重点检查：
1. 是否有安全隐患（注入、越权、密钥泄露）
2. 是否有未处理的边界情况
3. 是否符合 CLAUDE.md 里的项目规范

给出问题清单和修改建议。
```

之后在对话里输入 `/review`，就等于把上面这段话发出去——**一键触发标准化审查**。

## 4.2 带参数的命令

命令里可以用占位符接收参数。
比如 `.claude/commands/fix-issue.md`：

```markdown
读取 issue #$ARGUMENTS 的内容，定位相关代码，给出修复方案。
```

调用：`/fix-issue 123` → `$ARGUMENTS` 被替换成 `123`。

---

# 五、项目级 vs 用户级命令

和 `CLAUDE.md` 的分级思路一致：

- **项目级**：放项目里的 `.claude/commands/`，提交进 git，**全团队共享**（如团队统一的 review、release 流程）。
- **用户级**：放你的全局 `~/.claude/commands/`，**所有项目都能用**（如你个人的某套调试流程）。

团队规范用项目级，个人习惯用用户级。

---

# 六、常见错误

**错误 1：同一段长提示反复手打**
这是在浪费时间。**复用三次以上，就封装成命令。**

**错误 2：命令写得太死**
该用参数的地方写死了，换个 issue 号就得改文件。
→ 用 `$ARGUMENTS` 等占位符。

**错误 3：个人命令塞进项目级**
你的私人流程提交进仓库会污染队友。→ 个人的放用户级。

**错误 4：从不 `/clear`，一个会话用到底**
上下文越堆越杂，回答质量下降、成本上升。
→ 换任务时 `/clear`。

---

# 七、最佳实践

1. **复用即封装**：一段提示用了几次，就做成命令。
2. **善用参数**：用占位符让命令通用，别写死。
3. **分级归位**：团队流程→项目级（共享），个人流程→用户级。
4. **命令名见名知意**：`/review`、`/fix-issue`、`/release`，一看就懂。
5. **换任务勤 `/clear`**：保持上下文干净。

---

# 八、动手实践：Demo 10 · 自定义 Slash 命令（可直接复制使用）

本 Demo 提供两个**开箱即用**的自定义命令文件。
把 `.claude/commands/` 整个复制到你的项目里，
就能用 `/review` 和 `/fix-issue`。

## 8.1 文件结构
```text
.claude/commands/
├── review.md       # /review     一键代码审查
└── fix-issue.md    # /fix-issue  按 issue 号定位并给修复方案（带参数）
```

## 8.2 怎么用
1. 把本目录的 `.claude/` 复制到你的项目根：
   ```bash
   cp -r .claude /你的项目/
   ```
2. 在该项目启动 `claude`，输入 `/` 能看到 `/review`、`/fix-issue`。
3. 试用：
   ```
   /review
   /fix-issue 123
   ```
`/fix-issue 123` 会把 `123` 填入命令里的 $ARGUMENTS。

## 8.3 区分项目级 / 用户级
- 团队共享 → 放项目 `.claude/commands/`（随 git 提交）
- 个人习惯 → 放 `~/.claude/commands/`（所有项目可用）

<!-- article-progressive-block:start -->
# 九、动手验证：先跑通 Slash 命令与自定义命令，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“Slash 命令与自定义命令”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 9.1 基线与候选只允许一个变量不同

验证“Slash 命令与自定义命令”时，先固定样本、基线、候选、成功标准和失败边界。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“Slash 命令与自定义命令”时，动作是：同环境运行基线与候选，记录输入、中间状态和异常。原始结果不能只保留截图或汇总分数，必须同步保存：可重放命令、结构化日志、输出 Diff、失败样本、版本，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 固定样本、基线、候选、成功标准和失败边界 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 通过阈值 | 结果符合结论条件，异常输入可解释、可恢复 |
| 立即停止 | 条件缺失、结果不可复现或失败后责任不清 |

## 9.2 执行前先排除不可比较条件

“Slash 命令与自定义命令”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“Slash 命令与自定义命令”的当前环境重复运行。
- 候选只改变一个与“Slash 命令与自定义命令”结论直接相关的条件。
- “Slash 命令与自定义命令”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “Slash 命令与自定义命令”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 9.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“Slash 命令与自定义命令”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 固定样本、基线、候选、成功标准和失败边界 |
| 过程可回放 | 同环境运行基线与候选，记录输入、中间状态和异常 |
| 结果可审计 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

“Slash 命令与自定义命令”的一次合格基线对照按以下顺序执行：

1. 保存“Slash 命令与自定义命令”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“Slash 命令与自定义命令”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“Slash 命令与自定义命令”：同环境运行基线与候选，记录输入、中间状态和异常。
4. 为“Slash 命令与自定义命令”保存：可重放命令、结构化日志、输出 Diff、失败样本、版本。
5. 使用“Slash 命令与自定义命令”预登记条件判断：结果符合结论条件，异常输入可解释、可恢复。
6. 如果“Slash 命令与自定义命令”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 十、用一张矩阵验证 Slash 命令与自定义命令 的关键结论

矩阵按正文顺序列出“Slash 命令与自定义命令”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| 什么是 Slash 命令 | 在对话里输入 / 开头的指令， | 只改变与“什么是 Slash 命令”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 常用内置命令 | 💡 /clear 很重要：一个会话里堆了太多无关任务，上下文会变乱、变贵。 | 只改变与“常用内置命令”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 重点：自定义命令——把重复流程封装起来 | 这才是 Slash 命令的威力所在。 | 只改变与“重点：自定义命令——把重复流程封装起来”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 项目级 vs 用户级命令 | 项目级：放项目里的 .claude/commands/，提交进 git，全团队共享（如团队统一的 review、release 流程）。 | 只改变与“项目级 vs 用户级命令”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 常见错误 | 错误 1：同一段长提示反复手打 | 只改变与“常见错误”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 最佳实践 | 复用即封装：一段提示用了几次，就做成命令。 -> 善用参数：用占位符让命令通用，别写死。 -> 分级归位：团队流程→项目级（共享），个人流程→用户级。 -> 命令名见名知意：/review、/fix-issue、/release，一看就懂。 | 只改变与“最佳实践”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

## 10.1 记录本次实际实验

下面的记录用于“Slash 命令与自定义命令”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "Slash 命令与自定义命令"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "同环境运行基线与候选，记录输入、中间状态和异常"
evidence: "可重放命令、结构化日志、输出 Diff、失败样本、版本"
pass_when: "结果符合结论条件，异常输入可解释、可恢复"
stop_when: "条件缺失、结果不可复现或失败后责任不清"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 10.2 边界实验必须证明能够停止和恢复

成功路径只能证明“Slash 命令与自定义命令”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：条件缺失、结果不可复现或失败后责任不清，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 | 结果符合结论条件，异常输入可解释、可恢复 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：条件缺失、结果不可复现或失败后责任不清 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：保留基线，缩小变量；根因确认前不扩大范围 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“Slash 命令与自定义命令”，第一步是：保留基线，缩小变量；根因确认前不扩大范围。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“Slash 命令与自定义命令”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 十一、Slash 命令与自定义命令 的结果解释

解释“Slash 命令与自定义命令”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 异常链路无法恢复 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“Slash 命令与自定义命令”只有同时满足“结果符合结论条件，异常输入可解释、可恢复”，并且没有出现“条件缺失、结果不可复现或失败后责任不清”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“Slash 命令与自定义命令”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“Slash 命令与自定义命令”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十二、Slash 命令与自定义命令 的发布判断

发布判断需要把“Slash 命令与自定义命令”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “Slash 命令与自定义命令”的基线与候选只存在一个计划内变量。
- [ ] “Slash 命令与自定义命令”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “Slash 命令与自定义命令”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “Slash 命令与自定义命令”的原始输出、中间状态和失败现场已经保留。
- [ ] “Slash 命令与自定义命令”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “Slash 命令与自定义命令”的停止条件、负责人和回滚入口已经演练。
- [ ] “Slash 命令与自定义命令”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“Slash 命令与自定义命令”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十三、总结

- **什么是 Slash 命令**：在对话里输入 / 开头的指令，
- **重点：自定义命令——把重复流程封装起来**：这才是 Slash 命令的威力所在。
- **怎么做**：自定义命令本质是一个 Markdown 文件，
- **实现机制**：就是 Slash 命令（斜杠命令）——一键触发某个功能或流程，

## 参考资料

- [Claude Code 文档](https://docs.anthropic.com/en/docs/claude-code/overview)
- [Claude Code 安全](https://docs.anthropic.com/en/docs/claude-code/security)
