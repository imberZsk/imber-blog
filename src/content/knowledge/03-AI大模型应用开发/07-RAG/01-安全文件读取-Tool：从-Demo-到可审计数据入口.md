# RAG（01） - 安全文件读取 Tool：从 Demo 到可审计数据入口

> 读完后，你应能：
> - 能验证“让模型“读取文件”并不难，难点是让它只能读取当前用户有权访问的文件，而且每次访问都能追踪”，并保存输入、输出与失败样本。
> - 能验证“生产系统不能把任意路径直接交给 open()：路径穿越、符号链接、超大文件和敏感后缀都可能把一个演示 Tool 变成数据泄露入口”，并保存输入、输出与失败样本。
> - 能验证“文件读取链路应把模型视为不可信调用方”，并保存输入、输出与失败样本。


让模型“读取文件”并不难，难点是让它只能读取当前用户有权访问的文件，而且每次访问都能追踪。生产系统不能把任意路径直接交给 `open()`：路径穿越、符号链接、超大文件和敏感后缀都可能把一个演示 Tool 变成数据泄露入口。

<!-- article-progressive-block:start -->
# 一、先建立全局：安全文件读取 Tool：从 Demo 到可审计数据入口 是什么？

理解“安全文件读取 Tool：从 Demo 到可审计数据入口”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“安全文件读取 Tool：从 Demo 到可审计数据入口”的第一个核心判断是：模型只负责提交 document_id，服务端负责解析真实路径、检查租户和 ACL、限制文件类型与大小、记录审计事件。。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | Why：Tool 是权限边界，不只是模型能力 | 模型只负责提交 document_id，服务端负责解析真实路径、检查租户和 ACL、限制文件类型与大小、记录审计事件。 |
| 2 | How：实现一个可运行的安全读取器 | 调用时，可信的 UserContext 必须来自鉴权中间件，绝不能让模型或浏览器自行提交 allowed_document_ids。 |
| 3 | 进入 RAG 前的数据契约 | 读取结果不应只返回一段字符串。 |
| 4 | What：验收与常见坑 | 用 ../secret、绝对路径、软链接和非法字符攻击 Tool，必须在读取前失败。 |
| 5 | 结论 | 安全文件 Tool 的核心不是 read_text()，而是可信身份、服务端 ID 映射、读取前授权、资源限制和审计。 |
| 6 | 让模型“读取文件”并不难 | 让模型“读取文件”并不难，难点是让它只能读取当前用户有权访问的文件，而且每次访问都能追踪。 |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["Why：Tool 是权限边界，不只是模型能力"] --> S2
  S2["How：实现一个可运行的安全读取器"] --> S3
  S3["进入 RAG 前的数据契约"] --> S4
  S4["What：验收与常见坑"] --> S5
  S5["结论"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“安全文件读取 Tool：从 Demo 到可审计数据入口”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“安全文件读取 Tool：从 Demo 到可审计数据入口”的对象和顺序已经明确后，再看可观察的失败：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、Why：Tool 是权限边界，不只是模型能力

文件读取链路应把模型视为不可信调用方。模型只负责提交 `document_id`，服务端负责解析真实路径、检查租户和 ACL、限制文件类型与大小、记录审计事件。不要允许模型传绝对路径，也不要把“模型不会乱传参数”当安全策略。

```mermaid
flowchart LR
    U[用户与身份令牌] --> A[Agent]
    A -->|document_id| T[File Tool]
    T --> P[租户与 ACL 校验]
    P --> R[安全路径解析]
    R --> L[大小与类型限制]
    L --> X[解析和脱敏]
    X --> C[Chunk 与来源元数据]
    T --> O[审计日志]
```


# 三、How：实现一个可运行的安全读取器

## 3.1 环境依赖

```text
# requirements.txt
pydantic>=2,<3
```

Python 3.10+ 示例只读取配置目录内的 UTF-8 文本文件。真实系统还要把 `allowed_document_ids` 换成数据库或权限服务查询，并把审计事件发送到不可篡改日志。

```python
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path

from pydantic import BaseModel, Field


# 单次允许返回的最大字节数，避免超大文件拖垮 Agent 请求。
MAX_FILE_BYTES = 256 * 1024
# Tool 允许解析的文本后缀，二进制格式应交给隔离解析服务。
ALLOWED_SUFFIXES = {".md", ".txt"}


class ReadFileInput(BaseModel):
    """定义模型可提交的最小参数，不接受真实文件路径。"""

    # 文档服务生成的稳定标识，不允许包含路径语义。
    document_id: str = Field(pattern=r"^[a-zA-Z0-9_-]{1,64}$")


@dataclass(frozen=True)
class UserContext:
    """保存调用用户的租户和可访问文档集合。"""

    # 当前请求所属租户，用于隔离审计和数据目录。
    tenant_id: str
    # 权限服务返回的可访问文档 ID 集合。
    allowed_document_ids: frozenset[str]


def read_authorized_file(
    request: ReadFileInput,
    user: UserContext,
    tenant_root: Path,
) -> str:
    """读取已授权文本文件；request 是模型参数，user 是可信身份上下文。"""

    # 规范化后的租户根目录，用于抵御相对路径和符号链接逃逸。
    resolved_root = tenant_root.resolve(strict=True)
    if request.document_id not in user.allowed_document_ids:
        # 权限失败不返回文件是否存在，避免泄露文档标识。
        raise PermissionError("document is not accessible")

    # 文档 ID 映射为受控文件名，模型无法注入目录片段。
    candidate_path = (resolved_root / f"{request.document_id}.md").resolve(strict=True)
    if candidate_path.parent != resolved_root:
        # resolve 后重新检查父目录，可阻止目录中的恶意符号链接。
        raise PermissionError("resolved path escaped tenant root")
    if candidate_path.suffix.lower() not in ALLOWED_SUFFIXES:
        raise ValueError("unsupported file type")

    # 文件元数据用于在实际读取前拒绝过大输入。
    file_size = candidate_path.stat().st_size
    if file_size > MAX_FILE_BYTES:
        raise ValueError("file exceeds size limit")

    # 严格 UTF-8 解码，异常文件进入隔离解析链路，不静默替换乱码。
    content = candidate_path.read_text(encoding="utf-8", errors="strict")
    return content
```

调用时，可信的 `UserContext` 必须来自鉴权中间件，绝不能让模型或浏览器自行提交 `allowed_document_ids`。

# 四、进入 RAG 前的数据契约

读取结果不应只返回一段字符串。后续解析和切分至少保留：`document_id`、`tenant_id`、`document_version`、`source_uri`、`mime_type`、`acl_groups`、`content_hash`。Chunk 继承这些字段，ES 与 VectorDB 才能在检索阶段下推权限条件。

# 五、What：验收与常见坑

- 用 `../secret`、绝对路径、软链接和非法字符攻击 Tool，必须在读取前失败。
- 无权限文档无论存在与否都返回同一类错误，日志中记录 `trace_id`，响应中不暴露路径。
- PDF、Office、图片不能在问答进程里直接解析；进入异步沙箱，设置页数、CPU、内存和超时上限。
- 日志只记文档 ID、权限判定和内容哈希，不记录原文、密钥或用户隐私。
- 重复读取可按 `tenant + document_id + version + parser_version` 缓存，权限摘要必须进入缓存键。

# 六、结论

安全文件 Tool 的核心不是 `read_text()`，而是可信身份、服务端 ID 映射、读取前授权、资源限制和审计。完成这层后，解析、Chunk、Embedding 和检索才有可靠的数据入口。

<!-- article-progressive-block:start -->
# 七、动手验证：先跑通 安全文件读取 Tool：从 Demo 到可审计数据入口，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“安全文件读取 Tool：从 Demo 到可审计数据入口”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 7.1 基线与候选只允许一个变量不同

验证“安全文件读取 Tool：从 Demo 到可审计数据入口”时，先固定标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“安全文件读取 Tool：从 Demo 到可审计数据入口”时，动作是：依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成。原始结果不能只保留截图或汇总分数，必须同步保存：原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 通过阈值 | 正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料 |
| 立即停止 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 |

## 7.2 执行前先排除不可比较条件

“安全文件读取 Tool：从 Demo 到可审计数据入口”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“安全文件读取 Tool：从 Demo 到可审计数据入口”的当前环境重复运行。
- 候选只改变一个与“安全文件读取 Tool：从 Demo 到可审计数据入口”结论直接相关的条件。
- “安全文件读取 Tool：从 Demo 到可审计数据入口”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “安全文件读取 Tool：从 Demo 到可审计数据入口”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 7.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“安全文件读取 Tool：从 Demo 到可审计数据入口”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份 |
| 过程可回放 | 依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成 |
| 结果可审计 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |

“安全文件读取 Tool：从 Demo 到可审计数据入口”的一次合格基线对照按以下顺序执行：

1. 保存“安全文件读取 Tool：从 Demo 到可审计数据入口”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“安全文件读取 Tool：从 Demo 到可审计数据入口”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“安全文件读取 Tool：从 Demo 到可审计数据入口”：依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成。
4. 为“安全文件读取 Tool：从 Demo 到可审计数据入口”保存：原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace。
5. 使用“安全文件读取 Tool：从 Demo 到可审计数据入口”预登记条件判断：正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料。
6. 如果“安全文件读取 Tool：从 Demo 到可审计数据入口”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 八、用一张矩阵验证 安全文件读取 Tool：从 Demo 到可审计数据入口 的关键结论

矩阵按正文顺序列出“安全文件读取 Tool：从 Demo 到可审计数据入口”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| Why：Tool 是权限边界，不只是模型能力 | 模型只负责提交 document_id，服务端负责解析真实路径、检查租户和 ACL、限制文件类型与大小、记录审计事件。 | 只改变与“Why：Tool 是权限边界，不只是模型能力”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| How：实现一个可运行的安全读取器 | 调用时，可信的 UserContext 必须来自鉴权中间件，绝不能让模型或浏览器自行提交 allowed_document_ids。 | 只改变与“How：实现一个可运行的安全读取器”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 进入 RAG 前的数据契约 | 读取结果不应只返回一段字符串。 | 只改变与“进入 RAG 前的数据契约”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| What：验收与常见坑 | 用 ../secret、绝对路径、软链接和非法字符攻击 Tool，必须在读取前失败。 | 只改变与“What：验收与常见坑”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 结论 | 安全文件 Tool 的核心不是 read_text()，而是可信身份、服务端 ID 映射、读取前授权、资源限制和审计。 | 只改变与“结论”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 让模型“读取文件”并不难 | 让模型“读取文件”并不难，难点是让它只能读取当前用户有权访问的文件，而且每次访问都能追踪。 | 只改变与“让模型“读取文件”并不难”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |

## 8.1 记录本次实际实验

下面的记录用于“安全文件读取 Tool：从 Demo 到可审计数据入口”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "安全文件读取 Tool：从 Demo 到可审计数据入口"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成"
evidence: "原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace"
pass_when: "正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料"
stop_when: "解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 8.2 边界实验必须证明能够停止和恢复

成功路径只能证明“安全文件读取 Tool：从 Demo 到可审计数据入口”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace | 正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“安全文件读取 Tool：从 Demo 到可审计数据入口”，第一步是：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“安全文件读取 Tool：从 Demo 到可审计数据入口”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 九、安全文件读取 Tool：从 Demo 到可审计数据入口 的结果解释

解释“安全文件读取 Tool：从 Demo 到可审计数据入口”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 先执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 |
| 异常链路无法恢复 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 先执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“安全文件读取 Tool：从 Demo 到可审计数据入口”只有同时满足“正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料”，并且没有出现“解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“安全文件读取 Tool：从 Demo 到可审计数据入口”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“安全文件读取 Tool：从 Demo 到可审计数据入口”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十、安全文件读取 Tool：从 Demo 到可审计数据入口 的发布判断

发布判断需要把“安全文件读取 Tool：从 Demo 到可审计数据入口”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的基线与候选只存在一个计划内变量。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的原始输出、中间状态和失败现场已经保留。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”的停止条件、负责人和回滚入口已经演练。
- [ ] “安全文件读取 Tool：从 Demo 到可审计数据入口”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“安全文件读取 Tool：从 Demo 到可审计数据入口”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十一、总结

- **Why：Tool 是权限边界，不只是模型能力**：模型只负责提交 document_id，服务端负责解析真实路径、检查租户和 ACL、限制文件类型与大小、记录审计事件。
- **How：实现一个可运行的安全读取器**：调用时，可信的 UserContext 必须来自鉴权中间件，绝不能让模型或浏览器自行提交 allowed_document_ids。
- **进入 RAG 前的数据契约**：读取结果不应只返回一段字符串。
- **What：验收与常见坑**：用 ../secret、绝对路径、软链接和非法字符攻击 Tool，必须在读取前失败。
- **结论**：安全文件 Tool 的核心不是 read_text()，而是可信身份、服务端 ID 映射、读取前授权、资源限制和审计。
- **环境依赖**：调用时，可信的 UserContext 必须来自鉴权中间件，绝不能让模型或浏览器自行提交 allowed_document_ids。

## 参考资料

- [LangChain Retrieval](https://docs.langchain.com/oss/python/langchain/retrieval)
- [Milvus 文档](https://milvus.io/docs)
