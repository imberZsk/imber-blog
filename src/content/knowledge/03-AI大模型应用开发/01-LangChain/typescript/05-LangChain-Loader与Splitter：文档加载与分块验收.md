# LangChain（05） - Loader 与 Splitter：加载文档并决定 chunk 大小

## TypeScript 实现地图

Loader 统一产出 `@langchain/core/documents.Document`；`@langchain/textsplitters.RecursiveCharacterTextSplitter` 使用 `chunkSize` 与 `chunkOverlap`。参数应由 Embedding 上限、段落长度和问题粒度共同决定。

```typescript runnable file=main.ts title="TypeScript chunk 参数检查" description="验证 overlap 并计算每块新增容量。"
const chunkSize = 800
const chunkOverlap = 120
if (chunkOverlap >= chunkSize) throw new Error('chunkOverlap 必须小于 chunkSize')
console.log({ chunkSize, chunkOverlap, newContentPerChunk: chunkSize - chunkOverlap })
```



> 读完后，你应能完成以下任务：
> - 绘制“RAG（12） - 知识库 Loader 与 Splitter：解析契约、清洗和分块验收 / Loader 的产物不只是字符串”的关键对象与数据流，解释“Loader 负责读取来源并尽量保留结构；”，并用源码位置、日志或 Trace 标注证据。
> - 为“RAG（12） - 知识库 Loader 与 Splitter：解析契约、清洗和分块验收 / 不同来源的解析重点”设计正常与异常输入，验证“代码缩进、否定词、表格表头和错误码大小写不能按普通空白处理。”，输出首个偏差位置与回归测试结果。
> - 实现“RAG（12） - 知识库 Loader 与 Splitter：解析契约、清洗和分块验收 / 结构优先的分块流程”的最小代码或配置，检验“先按标题、条款、函数、表格等结构形成语义单元。 -> 单元超出 Token 上限，再按段落、句子和字符递归降级。 -> 小块继承标题路径、页码、来源、版本、租户和 ACL。 -> 生成稳定 ID 和内容哈希，支持重复执行、更新与删除。”，输出命令、结果与 Diff，并说明不适用边界。

> 更新日期：2026/08/11

<!-- article-progressive-block:start -->
# 一、先建立全局：LangChain Loader 与 Splitter：加载文档并决定 chunk 大小 是什么？

LangChain 的 Loader 负责把 PDF、Markdown、网页或文件系统内容变成 `Document`，Splitter 再把长 `Document` 变成带 `pageContent` 和 `metadata` 的小块。`RecursiveCharacterTextSplitter` 通常从段落、换行、空格到字符逐级降级切分，优先保留语义边界；它不是“固定截取前 N 个字符”。

`chunkSize` 和 `chunkOverlap` 没有通用魔法值：先看 Embeddings 模型和下游 ChatModel 的 Token 上限，再用真实问题集比较召回率、上下文长度和重复率。中文字符数只是近似值，生产实验应使用目标模型 Tokenizer 校准。

理解“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的第一个核心判断是：Loader 负责读取来源并尽量保留结构；。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | Loader 的产物不只是字符串 | Loader 负责读取来源并尽量保留结构； |
| 2 | 不同来源的解析重点 | 代码缩进、否定词、表格表头和错误码大小写不能按普通空白处理。 |
| 3 | 结构优先的分块流程 | 先按标题、条款、函数、表格等结构形成语义单元。 -> 单元超出 Token 上限，再按段落、句子和字符递归降级。 -> 小块继承标题路径、页码、来源、版本、租户和 ACL。 -> 生成稳定 ID 和内容哈希，支持重复执行、更新与删除。 |
| 4 | 稳定 ID 与幂等 | 不要只使用 document_id#序号：文档头部插入新段落后， |
| 5 | 分块质量验收 | 解析成功率、空文档率、乱码率和 OCR 失败率。 |
| 6 | 常见故障与修复 | 引用回不到原文：Loader 丢了页码或 URI，修解析契约，不要让模型生成来源。 |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["Loader 的产物不只是字符串"] --> S2
  S2["不同来源的解析重点"] --> S3
  S3["结构优先的分块流程"] --> S4
  S4["稳定 ID 与幂等"] --> S5
  S5["分块质量验收"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的对象和顺序已经明确后，再看可观察的失败：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、Loader 的产物不只是字符串

Loader 负责读取来源并尽量保留结构；
Splitter 负责把规范化 Document 变成可检索证据。
两者分开，才能独立更换 PDF 解析器和分块策略。


# 三、不同来源的解析重点

| 来源 | 必须保留 | 常见污染 |
| --- | --- | --- |
| PDF | 页码、坐标、标题、表格、图片说明 | 页眉页脚、双栏错序、OCR 乱码 |
| Markdown | 标题树、列表、代码围栏、链接 | 导航目录、构建标记 |
| HTML | 正文 DOM、标题、URL、更新时间 | 菜单、广告、推荐和 Cookie 文案 |
| Word | 标题级别、表格、批注/修订策略 | 样式文本混入正文 |
| 数据库 | 主键、字段名、更新时间、权限 | 只导出展示值而丢字段语义 |

清洗只删能证明无业务价值的内容。
代码缩进、否定词、表格表头和错误码大小写不能按普通空白处理。

# 四、结构优先的分块流程

1. 先按标题、条款、函数、表格等结构形成语义单元。
2. 单元超出 Token 上限，再按段落、句子和字符递归降级。
3. 小块继承标题路径、页码、来源、版本、租户和 ACL。
4. 生成稳定 ID 和内容哈希，支持重复执行、更新与删除。
5. 小块保留 `parent_id`，在线命中后可回取完整父块。

固定字符切分可以作为数据流 Demo，但不应成为所有格式的统一生产方案。

# 五、稳定 ID 与幂等


不要只使用 `document_id#序号`：文档头部插入新段落后，
后续序号全部变化，
会产生大量无意义删除与重建。

# 六、分块质量验收

抽样阅读不可少，自动指标至少包括：

- 解析成功率、空文档率、乱码率和 OCR 失败率。
- Chunk P50/P95 Token、超限率和过短率。
- 句子/代码围栏截断率、标题继承率、表格破坏率。
- 重复 Chunk 率与稳定 ID 冲突率。
- 来源、页码、ACL 等必填 Metadata 缺失率。
- 用标注问题评估不同策略的 Recall@K，而不是只比较块长。

# 七、常见故障与修复

- **引用回不到原文**：Loader 丢了页码或 URI，修解析契约，不要让模型生成来源。
- **检索总命中页眉**：在解析阶段基于跨页重复率移除页眉页脚。
- **条款只召回结论不召回例外**：按条款结构切，或命中后扩展邻居/父块。
- **更新后旧块仍存在**：用文档版本计算新旧 ID 差集，并对所有索引执行删除。
- **权限串用**：ACL 从 Document 到 Chunk 全程继承，并在写入前做非空校验。

<!-- article-progressive-block:start -->
# 八、动手验证：先跑通 知识库 Loader 与 Splitter：解析契约、清洗和分块验收，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 8.1 基线与候选只允许一个变量不同

验证“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”时，先固定标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”时，动作是：依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成。原始结果不能只保留截图或汇总分数，必须同步保存：原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 通过阈值 | 正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料 |
| 立即停止 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 |

## 8.2 执行前先排除不可比较条件

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的当前环境重复运行。
- 候选只改变一个与“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”结论直接相关的条件。
- “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 8.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 标注查询集、原文与 chunk 快照、Embedding 和索引版本、权限身份 |
| 过程可回放 | 依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成 |
| 结果可审计 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的一次合格基线对照按以下顺序执行：

1. 保存“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”：依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成。
4. 为“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”保存：原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace。
5. 使用“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”预登记条件判断：正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料。
6. 如果“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 九、用一张矩阵验证 知识库 Loader 与 Splitter：解析契约、清洗和分块验收 的关键结论

矩阵按正文顺序列出“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| Loader 的产物不只是字符串 | Loader 负责读取来源并尽量保留结构； | 只改变与“Loader 的产物不只是字符串”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 不同来源的解析重点 | 代码缩进、否定词、表格表头和错误码大小写不能按普通空白处理。 | 只改变与“不同来源的解析重点”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 结构优先的分块流程 | 先按标题、条款、函数、表格等结构形成语义单元。 -> 单元超出 Token 上限，再按段落、句子和字符递归降级。 -> 小块继承标题路径、页码、来源、版本、租户和 ACL。 -> 生成稳定 ID 和内容哈希，支持重复执行、更新与删除。 | 只改变与“结构优先的分块流程”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 稳定 ID 与幂等 | 不要只使用 document_id#序号：文档头部插入新段落后， | 只改变与“稳定 ID 与幂等”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 分块质量验收 | 解析成功率、空文档率、乱码率和 OCR 失败率。 | 只改变与“分块质量验收”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |
| 常见故障与修复 | 引用回不到原文：Loader 丢了页码或 URI，修解析契约，不要让模型生成来源。 | 只改变与“常见故障与修复”相关的条件 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace |

## 9.1 记录本次实际实验

下面的记录用于“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "知识库 Loader 与 Splitter：解析契约、清洗和分块验收"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "依次回放解析、切分、召回、过滤、重排、上下文组装和引用生成"
evidence: "原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace"
pass_when: "正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料"
stop_when: "解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 9.2 边界实验必须证明能够停止和恢复

成功路径只能证明“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 原文位置、各路候选、分数、过滤前后 Diff、Recall@K、NDCG、引用和 Trace | 正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”，第一步是：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 十、知识库 Loader 与 Splitter：解析契约、清洗和分块验收 的结果解释

解释“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 先执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 |
| 异常链路无法恢复 | 解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论 | 先执行：在最早丢失正确证据的阶段停止，固定该阶段输入单独修复并重放全链 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”只有同时满足“正确证据可召回可回链，无答案时拒答，权限过滤不泄漏其他主体资料”，并且没有出现“解析丢内容、正确 chunk 未召回、过滤误删、重排掉出或引用不支持结论”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十一、知识库 Loader 与 Splitter：解析契约、清洗和分块验收 的发布判断

发布判断需要把“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的基线与候选只存在一个计划内变量。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的原始输出、中间状态和失败现场已经保留。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的停止条件、负责人和回滚入口已经演练。
- [ ] “知识库 Loader 与 Splitter：解析契约、清洗和分块验收”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“知识库 Loader 与 Splitter：解析契约、清洗和分块验收”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十二、总结

- **Loader 的产物不只是字符串**：Loader 负责读取来源并尽量保留结构；
- **不同来源的解析重点**：| 来源 | 必须保留 | 常见污染 |
- **结构优先的分块流程**：先按标题、条款、函数、表格等结构形成语义单元。 -> 单元超出 Token 上限，再按段落、句子和字符递归降级。 -> 小块继承标题路径、页码、来源、版本、租户和 ACL。 -> 生成稳定 ID 和内容哈希，支持重复执行、更新与删除。
- **稳定 ID 与幂等**：不要只使用 document_id#序号：文档头部插入新段落后，后续序号全部变化，会产生大量无意义删除与重建。
- **分块质量验收**：抽样阅读不可少，自动指标至少包括：
- **常见故障与修复**：引用回不到原文：Loader 丢了页码或 URI，修解析契约，不要让模型生成来源。

## 可运行实验：观察 chunkSize 与 overlap

```typescript runnable file=main.ts title="TypeScript 分块参数观察" description="修改 chunkSize 和 overlap，观察分块边界与重复内容。"
/** 等待分块的字符序列。 */
const text = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'
/** 每个文本块包含的最大字符数。 */
const chunkSize = 10
/** 相邻文本块共享的字符数。 */
const chunkOverlap = 3
/** 每个窗口向前移动的字符数。 */
const step = chunkSize - chunkOverlap
/** 当前参数生成的全部文本块。 */
const chunks: string[] = []
for (let start = 0; start < text.length; start += step) {
  /** 当前窗口截取的文本。 */
  const chunk = text.slice(start, start + chunkSize)
  if (!chunk) break
  chunks.push(chunk)
  if (start + chunkSize >= text.length) break
}
console.log(chunks)
```

真实项目应把这个实验替换为 `RecursiveCharacterTextSplitter({ chunkSize, chunkOverlap })`，再用真实问题集比较召回率、重复率和上下文长度。

## 参考资料

- [LangChain Document loaders](https://docs.langchain.com/oss/javascript/integrations/document_loaders)
- [LangChain Text splitters](https://docs.langchain.com/oss/javascript/integrations/splitters)

## 参考资料

- [LangChain Retrieval](https://docs.langchain.com/oss/javascript/langchain/retrieval)
- [Milvus 文档](https://milvus.io/docs)
