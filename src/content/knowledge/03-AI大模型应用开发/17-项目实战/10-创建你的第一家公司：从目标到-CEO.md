# 项目实战（10） - 创建你的第一家公司：从目标到 CEO

> 读完后，你应能完成以下任务：
> - 绘制“项目实战（10） - 创建你的第一家公司：从目标到 CEO / 步骤 1：创建公司”的关键对象与数据流，解释“Description（描述）：这家公司是干嘛的（可选，但强烈建议写）。”，并用源码位置、日志或 Trace 标注证据。
> - 为“项目实战（10） - 创建你的第一家公司：从目标到 CEO / 步骤 2：设一个「好目标」”设计正常与异常输入，验证“因为 CEO Agent 会基于这个目标自动拆解出所有任务。”，输出首个偏差位置与回归测试结果。
> - 实现“项目实战（10） - 创建你的第一家公司：从目标到 CEO / 步骤 3：招聘 CEO Agent”的最小代码或配置，检验“CEO 是你创建的第一个 Agent，也是整个组织的根节点。”，输出命令、结果与 Diff，并说明不适用边界。

> 本章目标：在 UI 里建出一家公司、写好目标、招出第一个 CEO Agent、搭起组织架构、设好预算，最后点「启动」让公司跑起来。这是你第一次真正动手「开公司」。

进阶篇从这里开始动手。
前面四章建立了认知，现在把空控制台变成一家能干活的公司。

---

<!-- article-progressive-block:start -->
# 一、先建立全局：创建你的第一家公司：从目标到 CEO 是什么？

理解“创建你的第一家公司：从目标到 CEO”，先要把标题中的对象放进同一条处理链：它接收什么输入，经过哪些状态变化，最终用什么证据判断结果。下表不另造概念，只把作者正文已经解释的章节按依赖顺序连起来。

“创建你的第一家公司：从目标到 CEO”的第一个核心判断是：Description（描述）：这家公司是干嘛的（可选，但强烈建议写）。。先弄清这个判断中的对象和输入输出，后面的实现、故障和验收才有共同语境。

| 顺序 | 章节 | 读完本节应抓住的结论 |
| --- | --- | --- |
| 1 | 步骤 1：创建公司 | Description（描述）：这家公司是干嘛的（可选，但强烈建议写）。 |
| 2 | 步骤 2：设一个「好目标」 | 因为 CEO Agent 会基于这个目标自动拆解出所有任务。 |
| 3 | 步骤 3：招聘 CEO Agent | CEO 是你创建的第一个 Agent，也是整个组织的根节点。 |
| 4 | 步骤 4：搭组织架构 | 组织树是严格层级——每个 Agent 只向一个经理汇报。 |
| 5 | 步骤 5：设预算 | 这是防止 AI 团队烧钱失控的最后一道闸（第 12 章细讲成本）。 |
| 6 | 步骤 6：启动 | CEO 在第一次心跳醒来。 -> 它读公司目标，制定一份策略，提交给你审批（这是治理闸门）。 -> 你批准后，CEO 把目标拆成任务，分配给下属（如果还没下属，它会申请雇人，又走一道审批）。 -> 下属在各自心跳里签出任务、干活。 |

## 1.1 核心对象之间怎样衔接

```mermaid
flowchart LR
  S1["步骤 1：创建公司"] --> S2
  S2["步骤 2：设一个「好目标」"] --> S3
  S3["步骤 3：招聘 CEO Agent"] --> S4
  S4["步骤 4：搭组织架构"] --> S5
  S5["步骤 5：设预算"]
```

这张图只表达本文的讲解顺序，不替代正文机制。判断“创建你的第一家公司：从目标到 CEO”是否真正掌握，需要能从最后一个结果沿图回到前面每个章节的输入、状态变化和证据。

## 1.2 再看失败：问题最早会出现在哪一步？

在“创建你的第一家公司：从目标到 CEO”的对象和顺序已经明确后，再看可观察的失败：条件缺失、结果不可复现或失败后责任不清。定位时不从最后一条错误猜原因，而是沿上图找第一个偏离正文结论的节点。
<!-- article-progressive-block:end -->

# 二、整体六步

```text
① 建公司  →  ② 设目标  →  ③ 招 CEO  →  ④ 搭组织  →  ⑤ 设预算  →  ⑥ 启动
```

---

# 三、步骤 1：创建公司

在 Web UI 点「New Company」，填两项：

- **Name（名字）**：你公司的名字。
- **Description（描述）**：这家公司是干嘛的（可选，但强烈建议写）。

> 公司是顶层单元，之后所有 Agent、任务、目标、预算都挂在它下面。

---

# 四、步骤 2：设一个「好目标」

每家公司都需要一个目标——所有工作追溯回去的「北极星」。
**目标的质量直接决定 AI 团队的产出质量。
**

好目标的标准：**具体、可衡量**。

| ❌ 坏目标 | ✅ 好目标 |
|----------|----------|
| 做个好产品 | 3 个月内做出 #1 的 AI 笔记 App，达到 $1M MRR |
| 搞点营销 | 建一个营销代理公司，Q2 前服务 10 个客户 |
| 做个社区 | 做一个 AI Agent 开发者学习与分享社区，提供课程、技术讨论和热文排行 |

到 Goals 区域，创建你的顶层公司目标。

> 💡 为什么这么强调目标？因为 CEO Agent 会**基于这个目标**自动拆解出所有任务。目标含糊，拆出来的任务就含糊，整支团队都在做无用功。这就是第 01 章说的「你的品味是天花板」。

---

# 五、步骤 3：招聘 CEO Agent

CEO 是你创建的**第一个 Agent**，也是整个组织的根节点。
配置项：

- **Name**：比如 "CEO"。
- **Role**：`ceo`。
- **Adapter（适配器）**：这个 Agent 用什么大脑跑。**Claude Local 是个好默认**（第 06 章详解各适配器）。
- **Prompt template（提示词模板）**：每次心跳给 CEO 的指令。
- **Budget（预算）**：每月花费上限，单位是**分（cents）**。

CEO 的 prompt 应该指示它：**review 公司健康度、制定策略、把工作委派给下属。
**

> 🧠 为什么 CEO 适合用 Claude Code？因为 CEO 干的活是「规划任务、拆解目标、指挥下属」——这正是 Claude Code 这类能做多步规划、能调工具的 Agent 最擅长的。简单脚本（process 适配器）做不了这种活。

## 5.1 CEO 的 prompt 模板可以用变量

`claude_local` 适配器的 prompt 模板支持 `{{变量}}` 替换：

| 变量 | 值 |
|------|-----|
| `{{agentId}}` | Agent 的 ID |
| `{{companyId}}` | 公司 ID |
| `{{runId}}` | 当前运行 ID |
| `{{agent.name}}` | Agent 名字 |
| `{{company.name}}` | 公司名字 |

这样一套模板能复用给不同 Agent，运行时自动填入各自的身份。

---

# 六、步骤 4：搭组织架构

从 CEO 出发，创建直接下属：

- **CTO** → 管工程类 Agent
- **CMO** → 管市场类 Agent
- 其他高管按需添加

每个 Agent 都有自己的适配器配置、角色、预算。
组织树是**严格层级**——每个 Agent 只向一个经理汇报。

> ⚠️ 别一上来就铺满组织。先 CEO 一个人跑通，确认闭环，再让 CEO 自己去申请雇人（雇人会走审批，见第 09 章）。从小开始是铁律。

---

# 七、步骤 5：设预算

在**公司级**和**单 Agent 级**都设每月预算。
Paperclip 会强制执行：

- **软告警（soft alert）**：用到 80% 时提醒。
- **硬停（hard stop）**：用到 100% 时——**Agent 被自动暂停**（状态变 `paused`）。

> 预算单位是「分」。比如想给某 Agent 每月 $50 上限，就填 `5000`。这是防止 AI 团队烧钱失控的最后一道闸（第 12 章细讲成本）。

---

# 八、步骤 6：启动

给 Agent 开启心跳，它们就开始干活了。
从看板（dashboard）监控进度。

启动后典型的第一幕：

1. CEO 在第一次心跳醒来。
2. 它读公司目标，制定一份策略，**提交给你审批**（这是治理闸门）。
3. 你批准后，CEO 把目标拆成任务，分配给下属（如果还没下属，它会申请雇人，又走一道审批）。
4. 下属在各自心跳里签出任务、干活。
5. 你在看板上实时看到「谁在干什么、花了多少」。

---

# 九、常见错误

- ❌ **目标写得太虚**
→ 「做个好社区」这种目标，CEO 拆不出有效任务。
务必具体可衡量。

- ❌ **第一步就建一大堆 Agent**
  → 组织铺太大，协作关系还没理顺就乱套。先 CEO 跑通再扩。

- ❌ **给 CEO 用了不会规划的适配器（如 process）**
→ CEO 要做多步规划和委派，
得用 `claude_local` / `codex_local` 这类有 agentic 能力的。
process 只适合执行固定命令。

- ❌ **预算填错单位**
→ 单位是「分」不是「元」。
想要 $50 要填 `5000`，填 `50` 就只有 $0.5。

- ❌ **忘了 CEO 的策略需要你审批才会往下走**
→ 公司「不动」时，先看是不是有个待审批卡在那等你点头（第 09 章）。

---

# 十、最佳实践

- ✅ **目标里写清「成功长什么样」**：给可衡量的指标（MRR、客户数、发布数量），CEO 才好对齐。
- ✅ **CEO 的 prompt 里写明它的职责边界**：review 健康度、定策略、委派——别让它自己埋头写代码。
- ✅ **预算先保守后放开**：初期设小预算观察 burn rate，跑顺了再调高。
- ✅ **喂「品味输入」**：把品牌指南、参考案例、风格要求放进 CEO 或相关 Agent 的上下文/项目目录，产出质量立刻不一样。

---

<!-- article-progressive-block:start -->
# 十一、动手验证：先跑通 创建你的第一家公司：从目标到 CEO，再改变一个变量

前面的章节已经建立问题、概念和机制。现在把“创建你的第一家公司：从目标到 CEO”放进同一套基线中运行；本节不再引入新术语，只验证前文结论能否被复现。

## 11.1 基线与候选只允许一个变量不同

验证“创建你的第一家公司：从目标到 CEO”时，先固定样本、基线、候选、成功标准和失败边界。候选方案只能改变本次要验证的变量；如果同时更换数据、依赖和配置，即使结果改善，也不能知道是哪一项产生作用。

执行“创建你的第一家公司：从目标到 CEO”时，动作是：同环境运行基线与候选，记录输入、中间状态和异常。原始结果不能只保留截图或汇总分数，必须同步保存：可重放命令、结构化日志、输出 Diff、失败样本、版本，使下一次复查可以在同一输入上重放。

| 实验要素 | 本文要求 |
| --- | --- |
| 固定条件 | 固定样本、基线、候选、成功标准和失败边界 |
| 唯一变量 | 本次候选方案与基线之间的一项明确差异 |
| 原始证据 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 通过阈值 | 结果符合结论条件，异常输入可解释、可恢复 |
| 立即停止 | 条件缺失、结果不可复现或失败后责任不清 |

## 11.2 执行前先排除不可比较条件

“创建你的第一家公司：从目标到 CEO”开始前先确认下面四项；任一项不成立，都应先修复实验条件，而不是解释结果。

- 基线能够在“创建你的第一家公司：从目标到 CEO”的当前环境重复运行。
- 候选只改变一个与“创建你的第一家公司：从目标到 CEO”结论直接相关的条件。
- “创建你的第一家公司：从目标到 CEO”的基线和候选使用同一批输入、同一版本依赖与同一通过阈值。
- “创建你的第一家公司：从目标到 CEO”的原始输出和失败现场不会被重试、格式化或汇总覆盖。

## 11.3 执行后先核对证据完整性

结果出来后先检查证据，再讨论“创建你的第一家公司：从目标到 CEO”是否通过。缺少中间状态时，最终输出只能说明现象，不能证明机制。

| 检查项 | 当前文章的判定 |
| --- | --- |
| 输入可追溯 | 固定样本、基线、候选、成功标准和失败边界 |
| 过程可回放 | 同环境运行基线与候选，记录输入、中间状态和异常 |
| 结果可审计 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

“创建你的第一家公司：从目标到 CEO”的一次合格基线对照按以下顺序执行：

1. 保存“创建你的第一家公司：从目标到 CEO”基线版本及输入摘要，确认基线本身可以重复运行。
2. 写下“创建你的第一家公司：从目标到 CEO”候选方案唯一变化的变量，以及它预期影响的指标。
3. 在同一环境执行“创建你的第一家公司：从目标到 CEO”：同环境运行基线与候选，记录输入、中间状态和异常。
4. 为“创建你的第一家公司：从目标到 CEO”保存：可重放命令、结构化日志、输出 Diff、失败样本、版本。
5. 使用“创建你的第一家公司：从目标到 CEO”预登记条件判断：结果符合结论条件，异常输入可解释、可恢复。
6. 如果“创建你的第一家公司：从目标到 CEO”未通过，不修改第二个变量，先恢复基线并保留失败现场。

# 十二、用一张矩阵验证 创建你的第一家公司：从目标到 CEO 的关键结论

矩阵按正文顺序列出“创建你的第一家公司：从目标到 CEO”的结论。一次实验只选择一行，只改变这一行对应的条件；不要把多行合并成一个无法归因的大实验。

| 正文章节 | 已解释的结论 | 本轮唯一变量 | 必须保存的证据 |
| --- | --- | --- | --- |
| 步骤 1：创建公司 | Description（描述）：这家公司是干嘛的（可选，但强烈建议写）。 | 只改变与“步骤 1：创建公司”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 步骤 2：设一个「好目标」 | 因为 CEO Agent 会基于这个目标自动拆解出所有任务。 | 只改变与“步骤 2：设一个「好目标」”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 步骤 3：招聘 CEO Agent | CEO 是你创建的第一个 Agent，也是整个组织的根节点。 | 只改变与“步骤 3：招聘 CEO Agent”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 步骤 4：搭组织架构 | 组织树是严格层级——每个 Agent 只向一个经理汇报。 | 只改变与“步骤 4：搭组织架构”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 步骤 5：设预算 | 这是防止 AI 团队烧钱失控的最后一道闸（第 12 章细讲成本）。 | 只改变与“步骤 5：设预算”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |
| 步骤 6：启动 | CEO 在第一次心跳醒来。 -> 它读公司目标，制定一份策略，提交给你审批（这是治理闸门）。 -> 你批准后，CEO 把目标拆成任务，分配给下属（如果还没下属，它会申请雇人，又走一道审批）。 -> 下属在各自心跳里签出任务、干活。 | 只改变与“步骤 6：启动”相关的条件 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 |

## 12.1 记录本次实际实验

下面的记录用于“创建你的第一家公司：从目标到 CEO”当前这一次实验，不是第二套知识目录。先从矩阵选择一个章节，再填写实际值；没有填写的字段表示尚未验证。

```yaml
topic: "创建你的第一家公司：从目标到 CEO"
selected_chapter: required
claim_from_article: required
baseline_version: required
changed_condition: exactly_one
execution: "同环境运行基线与候选，记录输入、中间状态和异常"
evidence: "可重放命令、结构化日志、输出 Diff、失败样本、版本"
pass_when: "结果符合结论条件，异常输入可解释、可恢复"
stop_when: "条件缺失、结果不可复现或失败后责任不清"
observed_result: required
first_deviation: null_or_evidence
recovery_replay: required_after_failure
```

## 12.2 边界实验必须证明能够停止和恢复

成功路径只能证明“创建你的第一家公司：从目标到 CEO”在当前样本上工作，不能证明它可以进入生产。边界实验需要主动制造：条件缺失、结果不可复现或失败后责任不清，并观察系统是否在产生不可逆副作用前停止。

| 场景 | 只改变什么 | 应保存什么 | 通过标准 |
| --- | --- | --- | --- |
| 正常路径 | 使用已知有效输入 | 可重放命令、结构化日志、输出 Diff、失败样本、版本 | 结果符合结论条件，异常输入可解释、可恢复 |
| 边界路径 | 把一个输入推进到约束临界值 | 临界值前后的输出与指标 | 不静默降级，不把部分结果冒充成功 |
| 明确失败 | 注入：条件缺失、结果不可复现或失败后责任不清 | 原始错误、首个异常阶段和最终状态 | 失败被正确分类且没有扩大副作用 |
| 恢复重放 | 执行：保留基线，缩小变量；根因确认前不扩大范围 | 原失败样本的复测证据 | 原样本恢复，正常样本没有回归 |

恢复动作不是简单重启。对于“创建你的第一家公司：从目标到 CEO”，第一步是：保留基线，缩小变量；根因确认前不扩大范围。完成后使用原始失败样本复测；只验证一个新样本成功，不能证明触发条件已经消失。

“创建你的第一家公司：从目标到 CEO”边界实验结束后，应把正常、临界、失败和恢复四类记录放在同一个运行批次中。这样才能区分“候选方案真的修复问题”和“环境变化让问题暂时没有出现”。

# 十三、创建你的第一家公司：从目标到 CEO 的结果解释

解释“创建你的第一家公司：从目标到 CEO”实验时先看首个偏差，而不是最后一条错误。最后的异常通常只是上游状态错误的结果；从末端反推容易误把症状当根因。

| 观察结果 | 可以支持的判断 | 下一步 |
| --- | --- | --- |
| 主链路没有达到预期 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 异常链路无法恢复 | 条件缺失、结果不可复现或失败后责任不清 | 先执行：保留基线，缩小变量；根因确认前不扩大范围 |
| 新样本成功但原样本仍失败 | 修复没有覆盖原始触发条件 | 固定原失败输入，恢复基线后重新比较 |
| 指标改善但证据无法回链 | 数据、版本或中间状态没有固定 | 暂停发布，补齐可追溯记录后重跑 |

“创建你的第一家公司：从目标到 CEO”只有同时满足“结果符合结论条件，异常输入可解释、可恢复”，并且没有出现“条件缺失、结果不可复现或失败后责任不清”，才可以认为主链路通过。这里的“通过”只对当前固定版本、样本和环境有效，不能外推到尚未测试的容量、权限或数据分布。

如果“创建你的第一家公司：从目标到 CEO”候选方案与基线差异很小，先检查证据分辨率是否足够；如果差异很大，先排除数据泄漏、环境漂移和版本不一致。两种情况都不能只看一个汇总均值，需要回到逐样本输出和中间状态。

“创建你的第一家公司：从目标到 CEO”故障定位完成后，记录“现象、首个偏差、根因、改动、原样本复测”五项。缺少原样本复测时，只能标记为待观察，不能标记为已解决。

# 十四、创建你的第一家公司：从目标到 CEO 的发布判断

发布判断需要把“创建你的第一家公司：从目标到 CEO”的质量、失败边界和恢复能力放在同一份记录中。以下任一条件缺失，都应停止扩量，而不是用“基本正常”替代证据。

- [ ] “创建你的第一家公司：从目标到 CEO”的基线与候选只存在一个计划内变量。
- [ ] “创建你的第一家公司：从目标到 CEO”的输入、代码、依赖、配置和数据版本可以追溯。
- [ ] “创建你的第一家公司：从目标到 CEO”的正常、临界、失败和恢复样本使用同一套断言。
- [ ] “创建你的第一家公司：从目标到 CEO”的原始输出、中间状态和失败现场已经保留。
- [ ] “创建你的第一家公司：从目标到 CEO”的日志、Trace、截图和测试数据已经脱敏。
- [ ] “创建你的第一家公司：从目标到 CEO”的停止条件、负责人和回滚入口已经演练。
- [ ] “创建你的第一家公司：从目标到 CEO”尚未覆盖的输入、权限、容量和外部依赖已经登记。

最终记录至少包含基线版本、唯一变量、原始证据、首个偏差、恢复复测和发布责任人。没有参与本次修改的人如果不能据此重放“创建你的第一家公司：从目标到 CEO”的判断，就不能发布。
<!-- article-progressive-block:end -->

# 十五、总结

- **步骤 1：创建公司**：Description（描述）：这家公司是干嘛的（可选，但强烈建议写）。
- **步骤 2：设一个「好目标」**：目标的质量直接决定 AI 团队的产出质量。
- **步骤 3：招聘 CEO Agent**：CEO 是你创建的第一个 Agent，也是整个组织的根节点。
- **步骤 4：搭组织架构**：组织树是严格层级——每个 Agent 只向一个经理汇报。
- **步骤 5：设预算**：这是防止 AI 团队烧钱失控的最后一道闸（第 12 章细讲成本）。
- **步骤 6：启动**：CEO 在第一次心跳醒来。 -> 它读公司目标，制定一份策略，提交给你审批（这是治理闸门）。 -> 你批准后，CEO 把目标拆成任务，分配给下属（如果还没下属，它会申请雇人，又走一道审批）。 -> 下属在各自心跳里签出任务、干活。

## 参考资料

- [FastAPI 大型应用](https://fastapi.tiangolo.com/tutorial/bigger-applications/)
- [Docker Compose](https://docs.docker.com/compose/)
