# 示例任务

请修复 `buggy-counter.js` 里的计数逻辑。

## 背景

这个函数用于统计购物车里的商品总数。每个 item 有：

- `name`：商品名称。
- `quantity`：商品数量。

## 要求

- `quantity` 为数字时，累加到总数。
- `quantity` 缺失时，当作 0。
- `quantity` 小于 0 时，忽略该项。
- 不引入第三方依赖。

## 验收

- 请说明你修改了什么。
- 请给出至少 3 个手动测试样例。
