## 环境

### jdk 版本

看自己电脑

`java -version：openjdk version "1.8.0_492"`

看项目，因为有个配置，要看父级的，在 /Users/imber/.m2/repository/com/huoyunren/cyt/cyt-parent/5.1.0-SNAPSHOT/cyt-parent-5.1.0-SNAPSHOT.pom

```xml
<parent>
    <groupId>com.huoyunren.cyt</groupId>
    <artifactId>cyt-parent</artifactId>
    <version>5.1.0-SNAPSHOT</version>
</parent>
```

```xml
<maven.compiler.source>1.8</maven.compiler.source>
<maven.compiler.target>1.8</maven.compiler.target>
```

Spring Cloud？

##

.m2

模块拆分，biz 为业务逻辑 + client 提供 feign 供其他服务调用 + common 公共 DTO，service 启动入口

cyt-openresty-gateway (API 网关)

## 常用平台

mysql

微服务

Eureka/Nacos

Feign

Apollo

定时任务

Kafka
